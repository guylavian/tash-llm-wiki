---
title: CrashLoopBackOff: payments-api OOMKilled — memory limit exhausted (2026-06-28)
type: question
question_tier: scenarios
domain: openshift
slug: openshift-kubepodcrashlooping
summary: Post-mortem — Container payments-api (namespace: payments, pod: payments-api-7d9c8f5b4-q2xkz) is being killed by the Linux OOM killer 
sources:
  - kb:concepts-pod-lifecycle
  - kb:microshift-rest-api-4-22-pod-v1
provenance:
  extracted: 2
  inferred: 1
  ambiguous: 0
status: draft
updated: 2026-06-28
---

# CrashLoopBackOff: payments-api OOMKilled — memory limit exhausted (2026-06-28)

> ⚠️ Out of corpus coverage — `openshift` holds `conceptual` only; this is a `scenarios` question and that tier is not ingested; verify against the primary source.

**Root cause.** Container payments-api (namespace: payments, pod: payments-api-7d9c8f5b4-q2xkz) is being killed by the Linux OOM killer on every startup cycle because its memory working set (~255 MiB) is within 1 MiB of its configured hard memory limit (256 MiB). Kubernetes restarts the container per its restart policy, but the application re-grows to the same footprint and is killed again, producing CrashLoopBackOff with 247 recorded restarts. The memory request (128 MiB) is set at half the limit, so the pod schedules and starts normally — the failure only manifests at runtime when the working set saturates the limit.

## Evidence
1. kube_pod_container_status_restarts_total{namespace="payments", pod="payments-api-7d9c8f5b4-q2xkz"} = 247
2. kube_pod_container_status_last_terminated_reason{...} = OOMKilled (value=1, active)
3. kube_pod_container_status_waiting_reason = (empty — pod not currently in CrashLoopBackOff waiting state; mid-restart-backoff window at query time)
4. kube_pod_status_phase{phase="Running"} = 1 — pod phase shows Running (briefly, between OOM kills)
5. container_memory_working_set_bytes = 267,386,880 bytes (~255.0 MiB)
6. kube_pod_container_resource_limits{resource="memory"} = 268,435,456 bytes (256 MiB) — working set is 99.6% of limit
7. kube_pod_container_resource_requests{resource="memory"} = 134,217,728 bytes (128 MiB) — scheduling succeeds; failure is runtime-only
8. KB doc concepts-pod-lifecycle confirms: kubelet restarts containers on failure per restartPolicy; OOMKilled is a terminal container state triggering the restart loop.

## See also
- [[concepts-pod-lifecycle]]
- [[microshift-rest-api-4-22-pod-v1]]

## Occurrences
- 2026-06-28 — Container payments-api (namespace: payments, pod: payments-api-7d9c8f5b4-q2xkz) is being killed by the Linux OOM killer on every startup cycle because its memory working set (~255 MiB) is within 1 MiB

## Sources
<!-- crosslink:begin (generated by crosslink.py — do not edit) -->
- [[concepts-pod-lifecycle|Pod Lifecycle]]
- [[microshift-rest-api-4-22-pod-v1|Pod v1]]
<!-- crosslink:end -->
