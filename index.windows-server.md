# windows-server — routing index

_Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `windows-server` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
**16 pages.**


## Topics (2)
- [[windows-server-implementation-review]] — The evaluation lens and Map of Content for the windows-server brain — a rule → anti-pattern → symptom checklist across storage/HCI, failover clustering, Hyper-V, remote/administration, and the authentication stack, plus a symptom → likely-cause reverse index.
- [[windows-server-overview]] — The spine of the windows-server brain — the server OS's core pillars (storage/HCI, failover clustering, Hyper-V virtualization, networking/DNS, remote/RDS, administration tooling, the Windows authentication stack) and how they compose into a hyperconverged or traditional datacenter deployment.

## Entities (12)
- [[dfs-replication]] — The Distributed File System Replication engine — replication groups, remote differential compression, and its role replicating both general file shares and AD's sysvol folder; contrasted with Storage Replica for disaster-recovery use.
- [[failover-cluster-quorum]] — Windows Server Failover Clustering's high-availability model and its quorum-voting mechanism — node votes, witness types (cloud/disk/file-share), dynamic quorum management, and how to force-start a cluster that has lost quorum.
- [[hyper-v]] — Microsoft's built-in type-1 hypervisor for Windows Server and Windows — VM virtualization with live migration, Hyper-V Replica disaster recovery, shielded VMs, and no extra licensing cost beyond the host edition.
- [[remote-desktop-services]] — The RDS role family for session-based and virtualized desktop/app hosting — Session Host, Connection Broker, Gateway, Web Access, and Licensing — and how they compose into a scalable farm; not available on Server Core.
- [[server-core]] — The minimal, GUI-less Windows Server installation option — smaller disk footprint and attack surface, managed via PowerShell/SConfig/RSAT/Windows Admin Center, with a defined subset of roles and role services (RD Session Host is notably excluded).
- [[storage-replica]] — Block-level, storage-agnostic volume replication for disaster recovery — synchronous (zero data loss) or asynchronous, in stretch-cluster, cluster-to-cluster, or server-to-server topologies, secured with Kerberos AES256 and SMB3 encryption.
- [[storage-spaces-direct]] — Software-defined storage that pools direct-attached drives across 2–16 clustered servers into a single, cache-tiered, erasure-coded storage pool — the storage layer of Windows Server hyperconverged infrastructure.
- [[windows-admin-center]] — The free, browser-based remote-management tool for Windows Server (physical, virtual, on-premises, or Azure) — connections, single sign-on/LAPS authentication, tags, hyperconverged-cluster management, and PowerShell script transparency.
- [[windows-server-credential-protection]] — OS/domain-level credential-theft mitigations introduced from Windows Server 2012 R2 onward — Restricted Admin mode for RDP, LSA protection, the Protected Users security group, and Authentication Policies/Silos — the layer immediately adjacent to LAPS-style local-admin password rotation.
- [[windows-server-dns-role]] — The DNS Server role in Windows Server — name resolution, Active Directory integration (domain-controller location), DNSSEC/dynamic updates/anycast, and deployment scenarios independent of AD DS.
- [[windows-server-kerberos-auth-stack]] — Windows' Kerberos v5 implementation — SSP/SSPI integration with Winlogon, the domain-controller KDC backed by AD DS, delegation, mutual authentication, and the Windows Server 2025 removal of the legacy SupportedEncryptionTypes registry key.
- [[wsus]] — The WSUS server role for centralized Microsoft-update distribution — upstream/downstream hierarchies, autonomous vs. replica mode, WID vs. SQL Server storage, and its deprecated-but-supported status.

## Questions (2)
- [[windows-server-rdsh-on-server-core]] — No — the RD Session Host role service is not included in the Server Core installation option (only RD Connection Broker, RD Licensing, and RD Virtualization Host are); a session-based RDS deployment needs Server with Desktop Experience, though the other RDS roles (Broker/Gateway/Web Access/Licensing) can still run on Server Core.
- [[windows-server-s2d-two-node-quorum]] — Yes — a 2-node Storage Spaces Direct cluster needs a witness (a file share witness, since S2D has no shared disk) because with only 2 node votes, rebooting either node leaves exactly 1 of 2 votes and the cluster stops; the witness supplies the tie-breaking third vote.
