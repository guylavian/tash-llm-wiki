# exchange — routing index

_Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `exchange` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
**13 pages.**


## Topics (3)
- [[exchange-ad-dependency-migration-restore]] — Exchange stores its own configuration and per-recipient pointers inside Active Directory, so AD schema/forest migrations, DC restores, and DC repointing all carry a real risk of silently breaking Exchange — a coupling five separate community threads hit without a clean, vendor-confirmed fix.
- [[exchange-implementation-review]] — The evaluation lens and Map of Content for the exchange brain — a rule → anti-pattern → symptom checklist across mail flow, queues, recipients, DAG/HA, client access, certificates, anti-spam/malware, and hybrid, plus a symptom → likely-cause reverse index.
- [[exchange-overview]] — The orientation page for the exchange brain — what on-premises Exchange Server (2016/2019/SE) is, its two-role architecture (Mailbox + Edge Transport), the transport/recipient/HA/client-access pillars, and where the corpus-backed reference tier lives.

## Entities (8)
- [[exchange-antispam-antimalware]] — The Content Filter agent's spam confidence level (SCL) thresholds and the built-in Malware Agent's antimalware policies that screen inbound/outbound mail on Exchange Mailbox and Edge Transport servers.
- [[exchange-certificates]] — The three certificate sourcing options (self-signed, internal CA, commercial CA) and the three identity-matching methods (subject, SAN, wildcard) Exchange 2016/2019/SE uses to secure client and server-to-server TLS.
- [[exchange-client-access-namespace]] — Why session affinity is no longer required for Exchange 2016/2019/SE client access, how Mailbox-to-Mailbox proxying replaced the standalone Client Access server role, and the load-balancing / Autodiscover implications.
- [[exchange-database-availability-groups]] — The DAG — up to 16 Mailbox servers grouped around a Windows failover cluster providing automatic database-level recovery — plus Active Manager, quorum, activation preference, and AutoDatabaseMountDial.
- [[exchange-hybrid-deployment]] — What a Microsoft 365/Office 365 hybrid deployment adds to on-premises Exchange — the cloud-based Hybrid Configuration Wizard, Microsoft Entra Connect for multi-forest sync, and Hybrid Modern Auth (HMA).
- [[exchange-mail-queues]] — The five queue types (delivery, poison, shadow, submission, unreachable) Exchange transport servers use to hold messages in flight, backed by a single ESE database, and the tools (Queue Viewer, Exchange Management Shell) used to inspect them.
- [[exchange-recipient-types]] — The explicit recipient types Exchange assigns via RecipientTypeDetails (mailboxes, contacts, groups, and the special Microsoft Exchange recipient) and why the distinction matters for management and permissions.
- [[exchange-transport-pipeline]] — The chain of services (Front End Transport, Transport, Mailbox Transport, and Edge Transport) and the Send/Receive connectors that move a message from sender to destination mailbox database in Exchange 2016/2019/SE.

## Questions (2)
- [[exchange-anonymous-relay-5-7-1]] — A device that can't authenticate (scanner, monitoring tool, internal app) gets 550 5.7.1 Unable to relay because it's hitting a default Receive connector that doesn't grant anonymous-relay permissions; the fix is a dedicated, IP-restricted anonymous-relay Receive connector on Front End Transport — never adding relay rights to a default connector.
- [[exchange-dag-quorum-loss-dismount]] — A DAG's databases all dismount simultaneously when the underlying Windows failover cluster loses quorum (e.g. witness server unreachable during a network partition) — this is by design, not corruption, and recovery requires restoring quorum before any database remounts.
