# palo-alto — raw source tier

IMMUTABLE ground truth for the `palo-alto` domain. Hand-authored notes go here (cite them as `note:_sources/palo-alto/<file>.md`); harvested PDFs land in `_raw/pdfs/` and scraped pages in `_raw/web/`. Never edit a note here to fix a synthesis page — fix the page.
