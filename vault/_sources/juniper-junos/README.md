# juniper-junos — raw source tier

IMMUTABLE ground truth for the `juniper-junos` domain. Hand-authored notes go here (cite them as `note:_sources/juniper-junos/<file>.md`); harvested PDFs land in `_raw/pdfs/` and scraped pages in `_raw/web/`. Never edit a note here to fix a synthesis page — fix the page.
