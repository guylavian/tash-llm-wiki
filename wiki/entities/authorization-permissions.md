---
title: Authorization permissions
type: entity
domain: keycloak
slug: authorization-permissions
summary: "A permission ties the object being protected ([[authorization-resources-scopes|resource or scope]]) to the set of [[authorization-policy-types|policies]] that must be evaluated to decide access — the glue between \"what\" and \"the conditions\". Conceptually: `X CAN DO Y ON RESOURCE Z`."
sources:
  - guide:authorization_services_guide
  - kb:authorization_services_guide/permission_overview
source_notes:
  - "[[rhbk-26-6-permission-overview]]"
provenance: needs-review
tags: [authz]
status: draft
updated: 2026-06-16
---

# Authorization permissions

**A permission ties the object being protected ([[authorization-resources-scopes|resource or scope]]) to the set of [[authorization-policy-types|policies]] that must be evaluated to decide access — the glue between "what" and "the conditions". Conceptually: `X CAN DO Y ON RESOURCE Z`.**

Managed on the **Permissions** tab when editing a resource server. Two kinds:

## Resource-based permission
Protects one or more whole resources with a set of policies. Fields: Name, Description, **Resources**, **Policy** (associate existing or create inline), **Decision Strategy**.
- **Apply To Resource Type** (typed permission): instead of listing instances, protect *all* resources of a given **Resource Type** with common policies (e.g. every banking account: owner-only, region-locked, specific auth method). Pairs with the resource `Type` field — see [[authorization-resources-scopes]].

## Scope-based permission
Protects specific **Scopes** (optionally restricted to one **Resource**; if no resource is chosen, all scopes are available) with a set of policies. Use this for per-action granularity rather than per-resource.

## Decision Strategy on a permission
Each permission carries a [[decision-strategies|Decision Strategy]] (`Unanimous` default, `Affirmative`, or `Consensus`) that combines the outcomes of its associated policies into the permission's decision. This is separate from the resource-server-wide Decision Strategy that resolves conflicts *between* permissions on the same resource/scope.

## Contradictions / caveats
- Stable across RHBK **26.0–26.6**.
- Watch the layered strategies: per-policy **Logic** → per-permission **Decision Strategy** → resource-server **Decision Strategy**. A conflict where one permission grants and another denies the same scope resolves per the *server* strategy (`Affirmative` grants, `Unanimous` denies).

## See also
- [[fine-grained-authorization]]
- [[authorization-policy-types]]
- [[authorization-resources-scopes]]
- [[decision-strategies]]
- [[policy-evaluation-tool]]

## Sources
<!-- crosslink:begin (generated by crosslink.py — do not edit) -->
- [[rhbk-26-6-permission-overview|Chapter 6. Managing permissions]]
<!-- crosslink:end -->
