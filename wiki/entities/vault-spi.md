---
title: Vault SPI
type: entity
domain: keycloak
slug: vault-spi
summary: "An SPI in the `org.keycloak.vault` package for writing a custom extension that connects RHBK to an arbitrary secrets-vault implementation; the built-in `files-plaintext` provider is the reference example."
sources:
  - guide:server_developer_guide
  - kb:https://docs.redhat.com/en/documentation/red_hat_build_of_keycloak/26.6/html/server_developer_guide/vault-spi
provenance: needs-review
tags: [server-config, spi]
status: draft
updated: 2026-06-16
---

# Vault SPI

**An SPI in the `org.keycloak.vault` package for writing a custom extension that connects RHBK to an arbitrary secrets-vault implementation; the built-in `files-plaintext` provider is the reference example.**

## Purpose

Lets RHBK resolve secret references (e.g. LDAP bind credentials, SMTP passwords, IdP client secrets) from an external vault rather than storing them inline. You implement the vault `Provider`/`ProviderFactory` pair in the `org.keycloak.vault` package and package it like any other extension. The bundled `files-plaintext` provider serves as the implementation template.

Deployment follows the standard flow — JAR into `providers/`, then `kc.sh build`. See [[spi-provider-model]] for packaging and the single-vs-multiple-implementation rules.

## Contradictions / caveats

- The dev guide chapter is brief and stable across RHBK 26.0–26.6; the built-in `files-plaintext` provider is the only one documented as an example. Consult server-configuration docs for the operational vault providers (file-based, KeyStore) and their config keys, which live outside the developer guide.

## See also
- [[spi-provider-model]]
- [[user-storage-spi]]

## Sources
<!-- crosslink:begin (generated by crosslink.py — do not edit) -->
- [[rhbk-26-6-vault-spi|Chapter 6. Vault SPI]]
<!-- crosslink:end -->
