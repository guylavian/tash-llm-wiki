---
title: Client Authentication Methods
type: entity
domain: keycloak
slug: client-authentication-methods
summary: "How a confidential client proves its identity to RHBK's token endpoint — secret, signed JWT, or signed JWT with the client secret"
sources:
  - guide:securing_applications_and_services_guide
  - kb:authz-client-
  - kb:oidc-layers-
provenance: needs-review
tags: [clients]
status: draft
updated: 2026-06-16
---

# Client Authentication Methods

**How a confidential client proves its identity to RHBK's token endpoint —
secret, signed JWT, or signed JWT with the client secret.**

By default the RHBK authorization client supports three ways to authenticate a
client to the server:

1. **Client ID and Client Secret** — the client sends its `client_id` and a
   shared `client_secret`. Simplest method; secret can be passed as form
   parameters or via HTTP Basic Auth.
2. **Client authentication with Signed JWT** — the client signs a JWT assertion
   with its **private key**; RHBK validates it against the registered public key.
   (`private_key_jwt`.)
3. **Client authentication with Signed JWT using the client secret** — the client
   signs a JWT using the **client secret** as an HMAC key. (`client_secret_jwt`.)

mTLS / certificate-bound client authentication is also available and is required
by stricter [[fapi-oauth21-profiles]].

## Public vs confidential
- **Public clients** (SPAs, native apps) cannot authenticate — they have no
  secure secret store. They identify themselves only by `client_id` and must use
  PKCE; the JavaScript adapter requires a public client. Consider [[dpop]] to
  sender-constrain their tokens.
- **Confidential clients** use one of the methods above and are required for the
  introspection, CIBA, and JWT-authorization-grant endpoints.

## Where it's enabled
Toggle **Client authentication** to On in the Admin Console, then configure
credentials on the **Credentials** tab (Client Id and Secret, or Signed JWT).
The token-exchange and direct-grant flows accept these same credentials as form
parameters, Basic Auth, client JWT, or client cert, per your realm's client
authentication flow.

## Contradictions / caveats
- Method names are stable across RHBK 26.0–26.6. FAPI Advanced and OAuth 2.1
  profiles can *require* stronger methods (private_key_jwt or mTLS) and forbid
  plain client-secret auth.

## See also
- [[oidc-grant-types]]
- [[dpop]]
- [[fapi-oauth21-profiles]]
- [[client-registration-service]]
- [[securing-apps-oidc-saml]]

## Sources
<!-- crosslink:begin (generated by crosslink.py — do not edit) -->
- [[rhbk-26-6-authz-client|Chapter 19. Red Hat build of Keycloak authorization client]]
- [[rhbk-26-6-oidc-layers|Chapter 2. Securing applications and services with OpenID Connect]]
<!-- crosslink:end -->
