# keycloak — routing index

_Generated by `_meta/bin/index.py` — do not hand-edit. The per-domain routing surface for the `keycloak` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
**157 pages.**


## Topics (26)
- [[access-token-validation-resource-server]] — A resource server must cryptographically verify every inbound access token — signature, expiry, issuer, audience, and scope — before granting any access; skipping or weakening any of these checks is the most common single point of privilege-escalation in OAuth2 APIs.
- [[authentication-flows]] — An authentication flow is an ordered container of executions and sub-flows (screens, authenticators, actions) that runs during login, registration, reset-credentials and brokering; requirement types on each element decide whether and how it runs.
- [[bff-token-handler]] — A Backend-for-Frontend (BFF) acts as a confidential OAuth client that runs the Authorization Code + PKCE flow on behalf of a SPA, stores all tokens server-side, and exposes only encrypted HttpOnly session cookies to the browser — eliminating token theft via XSS as an attack vector.
- [[fine-grained-authorization]] — RHBK Authorization Services turn a confidential client into a centralized policy decision point: you register protected [[authorization-resources-scopes|resources and scopes]], attach reusable [[authorization-policy-types|policies]] via [[authorization-permissions|permissions]], and enforce decisions with a [[policy-enforcer|Policy Enforcement Point]] — going beyond plain role checks to combine ABAC, RBAC, UBAC, CBAC, and rule-based access control.
- [[ha-cross-site]] — Running RHBK across multiple clusters/sites for resilience, using external Infinispan / Red Hat Data Grid for cross-site session replication
- [[identity-brokering]] — RHBK acts as an identity broker — an intermediary that delegates authentication to external identity providers (social, OIDC, SAML) and then issues its own token to the client, so applications only ever integrate with RHBK.
- [[jwt-validation-pitfalls]] — A catalogue of the ways JWT validation goes wrong — algorithm confusion, skipped signature checks, header-injection, and cross-token misuse — so reviewers can predict the faults a broken implementation will produce.
- [[ldap-user-federation]] — How RHBK's built-in LDAP/AD provider (User Storage SPI) turns directory users into RHBK users — governed by storage/import mode and edit mode, with LDAP mappers for attributes/groups/roles and a truststore for LDAPS.
- [[observability-stack]] — RHBK exposes built-in health probes, Prometheus/OpenMetrics metrics, and OpenTelemetry traces (plus preview OTel logs/metrics), most of it served on the dedicated management port `9000`, so a single deployment feeds a centralized observability stack with no extra sidecars.
- [[oidc-client-best-practices]] — How to write app code that talks to RHBK/Keycloak so flow choice, client auth, token refresh, validation, logout, and session loss behave correctly — RHBK-grounded rules plus RFC 9700 upstream best practice.
- [[operator-advanced-config]] — The advanced knobs on the [[keycloak-cr]] beyond DB/hostname/TLS: trusted certificates, Pod customization, scheduling, secret references, resources, ingress, and admin bootstrapping
- [[operator-deployment]] — The end-to-end path for running RHBK on OpenShift: install the Operator via OLM, provision DB/hostname/TLS prerequisites, then declare a `Keycloak` custom resource that the Operator reconciles into a StatefulSet, Service, and (optionally) Ingress
- [[production-checklist]] — Going to production with `kc.sh start` means satisfying RHBK's secure-by-default requirements: TLS, an explicit hostname, a real database, a proxy/cluster topology, and observability kept internal.
- [[realm-administration]] — A realm is the isolated security domain in which Keycloak/RHBK manages users, applications, roles, and groups; one deployment hosts many realms, all isolated from one another.
- [[rhbk-ha-architectures]] — RHBK ships two documented HA shapes: a single OpenShift cluster (optionally spread across availability zones) using embedded Infinispan, and a two-site Active/Passive deployment using an external Red Hat Data Grid with synchronous replication. The guide prioritizes consistency over availability
- [[rhsso-to-rhbk-migration]] — Moving from legacy Red Hat Single Sign-On 7.6 to Red Hat Build of Keycloak (RHBK), the supported successor product — and, by the same machinery, upgrading between RHBK versions
- [[saml-clients-and-migration]] — How SAML service-provider apps integrate with RHBK as IdP, which SAML adapters survive the RH-SSO→RHBK move, and what changes — distinct from the OIDC story
- [[securing-apps-oidc-saml]] — RHBK is an OAuth2 / OpenID Connect / SAML 2.0 compliant server; you secure an application by registering it as a *client* in a realm and having it speak one of those protocols, ideally via the framework's native support rather than a Keycloak adapter
- [[security-hardening-checklist]] — Consolidated mitigations from the \"Mitigating security threats\" chapter — host validation, TLS, brute force, token leakage, redirect URIs, FAPI/OAuth 2.1, and session limits — for hardening an RHBK realm/deployment.
- [[server-config-migration]] — Mapping each RH-SSO 7.6 `standalone.xml` subsystem (datasource, TLS, cache, hostname, truststore, vault, JVM) to the equivalent RHBK `kc.sh` options
- [[server-configuration]] — Red Hat build of Keycloak is configured from four ordered sources and distinguishes build-time options (baked into an optimized image) from runtime options (applied at `start`).
- [[spi-provider-model]] — Red Hat build of Keycloak (RHBK) is customized by implementing Service Provider Interfaces (SPIs): each extension supplies a `Provider` plus a `ProviderFactory`, registered through a `META-INF/services` file, packaged in a JAR dropped into `providers/`, and activated by running `kc.sh build`.
- [[sso-implementation-review]] — An evaluation lens for reviewing whether an SSO/OIDC integration meets current best-practice standards; maps every security concept to a best-practice rule, its anti-pattern, and the observable symptom (ticket) that anti-pattern produces — enabling both proactive checklists and fault-first root-cause lookup.
- [[terraform-keycloak-iac]] — Managing Keycloak/RHBK realms, clients, roles, IdPs and federation declaratively with the Terraform provider — and what that means for RH-SSO→RHBK migration and an air-gapped network
- [[tokens-and-sessions]] — How RHBK issues, scopes, and expires access/refresh tokens, and how user sessions and their timeouts are governed
- [[troubleshooting-index]] — Triage map for Red Hat build of Keycloak / RH-SSO, organized by area, linking the public-fix entity pages and pointing at the gated KB solutions

## Entities (125)
- [[3scale-rhsso-support]] — Which identity-provider product/version Red Hat 3scale API Management is tested and supported with for SSO integration
- [[ad-idp-link-loss-objectguid]] — Symptom → cause → fix for users sporadically losing their AD federated link and being asked to merge accounts.
- [[adapter-migration]] — Several RH-SSO 7.6 Java client adapters are no longer released with RHBK; re-platform applications onto the standards-based OIDC/SAML support in their own stack, and account for protocol/client-setting changes
- [[additional-options]] — The `spec.additionalOptions` field on the [[keycloak-cr]] — a key/value escape hatch for any server option that has no first-class field on the CR
- [[admin-console-confidential-lockout]] — Symptom → cause → fix for being locked out of the Admin UI after flipping `security-admin-console` to confidential.
- [[air-gapped-client-integration]] — What changes when the network has no internet: how client apps get their OIDC/ SAML libraries, validate tokens, and migrate RH-SSO→RHBK without reaching public endpoints
- [[audience-and-scope-checks]] — A resource server must reject any access token whose `aud` claim does not identify that server, and must enforce the scope/role claims before granting access to each operation; skipping either check enables token substitution, confused-deputy, and over-privilege attacks.
- [[authorization-permissions]] — A permission ties the object being protected ([[authorization-resources-scopes|resource or scope]]) to the set of [[authorization-policy-types|policies]] that must be evaluated to decide access — the glue between \"what\" and \"the conditions\". Conceptually: `X CAN DO Y ON RESOURCE Z`.
- [[authorization-policy-types]] — Policies define the *conditions* that must be satisfied to grant access, decoupled from what they protect — generic, reusable, and combinable. A [[authorization-permissions|permission]] binds one or more policies to a resource/scope.
- [[authorization-resources-scopes]] — A resource is the object being protected (a page, REST endpoint, file, EJB, or a group/type of them); a scope is a bounded action or attribute on that resource (view, edit, delete, cost…). Together they are the targets that [[authorization-permissions|permissions]] protect.
- [[authorization-server-metadata-discovery]] — The mechanism by which OAuth 2.0 / OIDC clients locate an authorization server's endpoints and capabilities via a well-known URL, with strict issuer validation required to prevent mix-up and impersonation attacks.
- [[back-channel-logout]] — A server-to-server logout mechanism in which the OpenID Provider POSTs a signed Logout Token directly to a registered RP endpoint, bypassing the browser entirely and making it the resilient alternative to front-channel/iframe logout in modern browsers.
- [[bearer-token-usage]] — RFC 6750 and the OAuth 2.0 Security BCP (RFC 9700) define strict rules for how access tokens are transmitted and handled; violating them causes token leakage, replay attacks, and silent access-control bypasses.
- [[bootstrap-admin-dns-query]] — Symptom → cause → fix for creating a temporary admin when the Kubernetes Infinispan stack has no DNS query.
- [[brute-force-detection]] — Realm feature (Realm settings → Security defenses → Brute Force Detection) that temporarily or permanently locks an account after repeated login failures; disabled by default.
- [[build-vs-runtime-options]] — Build options are persisted into an optimized RHBK image by `kc.sh build`; runtime (configuration) options are applied at `kc.sh start`.
- [[client-authentication-methods]] — How a confidential client proves its identity to RHBK's token endpoint — secret, signed JWT, or signed JWT with the client secret
- [[client-libraries-by-stack]] — Which library to use to authenticate an app against RHBK/Keycloak, by stack — and what the RH-SSO→RHBK move means for each
- [[client-registration-cli]] — `kcreg.sh` / `kcreg.bat` — the command-line front end to the RHBK [[client-registration-service]] REST endpoints, for self-service client config in scripts
- [[client-registration-service]] — REST API for self-service client registration, supporting OIDC Dynamic Client Registration, RHBK's native format, and SAML v2 Entity Descriptors — gated by Initial Access Tokens and Registration Access Tokens
- [[config-sources-precedence]] — RHBK reads options from four sources; when the same option is set in more than one, the highest-priority source wins.
- [[cors-for-spa]] — Defines what CORS headers an authorization server must emit for browser-based OAuth clients, and what CORS policy a resource server or BFF must enforce to prevent cross-origin data theft while still allowing legitimate SPA fetch calls.
- [[custom-keycloak-image]] — Pointing the [[keycloak-cr]] at a custom container image (`spec.image`) so the server runs a pre-augmented, build-time-optimized build instead of re-augmenting on every Pod start
- [[custom-provider-migration]] — Custom SPI providers must be recompiled for Jakarta EE 10 and the consolidated KeycloakSession API, deployed as plain JARs in `providers/` with a build step — and the AngularJS Admin/Account consoles have no theme migration path
- [[database-auto-migration]] — RHBK migrates the relational database schema automatically the first time it connects to the database; a manual mode lets you export the SQL and apply it yourself
- [[database-configuration]] — RHBK stores realm/user/client data in a relational database selected by the build-time `db` option, with connection details (`db-url-host`, `db-username`, `db-password`) set at runtime.
- [[decision-strategies]] — Decision strategies say how multiple positive/negative outcomes combine into one decision; enforcement mode says what happens when no policy applies at all.
- [[distributed-caches]] — RHBK's clustering/session layer is an in-memory Infinispan data grid. Caches live either embedded in each RHBK pod (single cluster) or in an external Data Grid cluster (cross-site)
- [[dpop]] — RFC 9449 mechanism that turns Bearer tokens into *sender-constrained* tokens by binding them to a client's public/private key pair, so a stolen token is useless without the private key
- [[dpop-sender-constraining]] — DPoP (Demonstrating Proof of Possession, RFC 9449) sender-constrains access and refresh tokens by binding them to a client-held asymmetric key pair, so a stolen token alone cannot be replayed without the matching private key.
- [[event-metrics]] — Event metrics expose aggregated counters of user activities (logins, login failures, token refreshes) as `keycloak_user_events_total`, per RHBK instance, on the standard `/metrics` endpoint.
- [[external-data-grid-operator]] — The multi-cluster building block that holds RHBK session caches in a separate Red Hat Data Grid cluster per site and replicates them synchronously between sites using Data Grid Cross-DC. RHBK connects to it as a remote store
- [[fapi-oauth21-profiles]] — Built-in RHBK Client Policy profiles that make the authorization server enforce FAPI 1/2 and OAuth 2.1 conformance on clients
- [[fapi2-security-profile]] — The OpenID Foundation FAPI 2.0 Security Profile is a hardened OAuth 2.0 / OIDC profile for high-value APIs, mandating PAR, PKCE S256, sender-constrained tokens, and strong client authentication; deviations surface as conformance failures, invalid_grant errors, or stolen-token reuse.
- [[feature-flags]] — Optional, preview, and deprecated functionality is toggled with the build-time `features` / `features-disabled` options.
- [[fips-startup-bouncycastle]] — Two distinct FIPS-mode failures: a native BC link error at startup, and an \"Invalid credentials\" admin login due to an Argon2 hash.
- [[grafana-dashboards]] — RHBK provides official Grafana dashboard JSON (troubleshooting and capacity planning) in the `keycloak/keycloak-grafana-dashboard` GitHub repo, imported into Grafana against a Prometheus datasource.
- [[ha-load-balancer-failover]] — The multi-cluster load-balancer building block: an AWS Global Accelerator with a static anycast IP routes client traffic to both sites, health-checks the `/lb-check` endpoint, and fails over to the healthy site on outage
- [[health-endpoints]] — RHBK exposes four REST health endpoints on the management port `9000`, returning `200 OK`/`503` plus a JSON `status`, for wiring Kubernetes startup/liveness/readiness probes.
- [[hostname-v2]] — The `hostname:v2` model controls how RHBK forms its frontend, backchannel, and admin base URLs (driving token issuer, OIDC discovery, redirect URIs, email links); production requires an explicit `--hostname` or `--hostname-strict false`.
- [[issuer-identification-mixup]] — When a client talks to more than one authorization server, it must verify that the authorization response came from the AS it actually sent the request to — failing this check exposes the flow to the OAuth 2.0 mix-up attack, where a code issued by an honest AS is redeemed at an attacker-controlled AS.
- [[javascript-providers-scripts]] — RHBK can run JavaScript at runtime for four customization points — Authenticator, JavaScript Policy, OIDC protocol mapper, SAML protocol mapper — but scripts are Technology Preview, disabled by default, and must be deployed as a JAR with a `keycloak-scripts.json` descriptor; you cannot upload a bare `.js` file.
- [[kc-bootstrap-admin]] — RHBK's `bootstrap-admin` start flags (or env vars) mint a temporary admin user or service account to get into a realm on first start or recovery; you then create a permanent admin and remove it.
- [[kcadm-cli]] — The command-line client (`kcadm.sh` on Linux, `kcadm.bat` on Windows) that performs Admin REST API CRUD operations against realms, clients, users, roles, groups, keys, and events.
- [[keycloak-cr]] — The `Keycloak` custom resource — the declarative spec the RHBK Operator reconciles into a StatefulSet, Service, and optional Ingress
- [[keycloak-realm-import]] — A `KeycloakRealmImport` custom resource that drives the Operator to import a realm into a managed Keycloak deployment via a one-shot Kubernetes Job
- [[keycloak-session-spi]] — `KeycloakSession` is the per-request handle passed into every provider; it is how an extension looks up other providers, the current realm/context, and runtime services.
- [[keycloak-themes]] — RHBK themes customize the look of user-facing and admin pages via Freemarker templates, message bundles, stylesheets and `theme.properties`; theme behavior itself is pluggable through the Theme Selector and Theme Resource SPIs.
- [[keycloak-truststores]] — The `spec.truststores` field on the [[keycloak-cr]] — supplies trusted CA certificates to the RHBK server from Kubernetes Secrets
- [[keycloak-vault]] — RHBK ships two Vault SPI implementations — a plaintext file-based vault (ideal for Kubernetes/OpenShift mounted secrets) and a Java KeyStore-based vault — referenced from realm config via `${vault.<name>}` placeholders.
- [[ldap-mappers]] — Components attached to an LDAP provider that map LDAP data (attributes, groups, roles) onto RHBK user attributes, group memberships, and role mappings
- [[ldap-storage-mode]] — Two settings on an LDAP provider that decide (a) whether users are copied into RHBK's database and (b) whether changes write back to LDAP
- [[management-interface]] — Health (`/health`) and metrics (`/metrics`) are served on a separate management HTTP server (default port 9000) so they can be kept off the public-facing port.
- [[management-port]] — RHBK serves observability endpoints — `/health/*` and `/metrics` — on a dedicated management interface, port `9000` by default, separate from the application HTTP(S) ports.
- [[managing-users-credentials]] — Creating and administering users, their profile attributes, and their credentials within a realm (Admin Console \"Users\", or `kcadm.sh`); avoid creating end users in the master realm.
- [[metrics-exemplars]] — Exemplars attach the last recorded trace ID to a metric data point, so a Grafana metrics panel can jump straight to the trace behind an outlier latency or error.
- [[mod-auth-mellon]] — `mod_auth_mellon` is an Apache HTTP Server module that turns httpd into a SAML 2.0 Service Provider, letting RHBK/Keycloak secure any application sitting behind that web server without embedding a Keycloak adapter in the app itself
- [[mtls-bound-tokens]] — mTLS certificate binding sender-constrains OAuth2 access (and optionally refresh) tokens to the client's TLS certificate thumbprint, making a stolen token useless without the matching private key. RFC 8705 defines the mechanism; FAPI 2.0 mandates it (alongside DPoP) for all access tokens.
- [[multi-site-feature-flag]] — An RHBK feature that must be enabled on the `Keycloak` CR for a multi-cluster/cross-site deployment; among other things it exposes the load-balancer probe `/lb-check`
- [[native-app-oauth]] — Rules and failure modes for OAuth 2.0 flows in mobile and desktop apps: use the system browser, PKCE mandatory, public-client registration, verified redirect URIs, and multi-AS mix-up defense.
- [[oidc-endpoints]] — The standard set of OpenID Connect / OAuth2 endpoints RHBK exposes per realm, all discoverable from the `.well-known/openid-configuration` document
- [[oidc-grant-types]] — The flows RHBK supports for obtaining tokens, and which are recommended vs. discouraged under current OAuth security best practice
- [[oidc-logout]] — How RHBK terminates a user's SSO session — via the OIDC logout endpoint (browser-redirect or direct call) or the SAML single-logout flow
- [[oidc-token-validation]] — The two ways a resource server checks an RHBK access token: offline signature verification via JWKS, or an online call to the token introspection endpoint
- [[opentelemetry-centralization]] — Global `telemetry-*` server options point all RHBK observability signals (Logs, Metrics, Traces) at a single OpenTelemetry collector, so you get one OTLP endpoint for the whole stack.
- [[operator-cr-migration]] — The RHBK Operator is a complete rewrite, not backward compatible with the RH-SSO 7.6 Operator — migration means installing it fresh and authoring new `Keycloak` CRs
- [[operator-ingress]] — How the Operator exposes RHBK: the built-in `spec.ingress` and the `spec.proxy.headers` setting on the [[keycloak-cr]]
- [[operator-initial-admin]] — How the Operator provisions the first admin: the `spec.bootstrapAdmin` stanza on the [[keycloak-cr]], or an auto-generated `<cr-name>-initial-admin` Secret
- [[operator-olm-install]] — Installing the `rhbk-operator` through OpenShift's Operator Lifecycle Manager — from OperatorHub or a `Subscription`, with manual upgrade approval and air-gapped catalog mirroring for disconnected clusters
- [[operator-pod-template]] — A raw Deployment pod-template override on the [[keycloak-cr]], merged into what the Operator generates — the escape hatch when no first-class CR field exists. Tech Preview / unsupported
- [[operator-proxy-port-required]] — Symptom → cause → fix for the operator pod refusing to start behind a cluster-wide proxy that omits the port.
- [[operator-resources]] — The `spec.resources` field on the [[keycloak-cr]] — compute requests/limits for the RHBK container, set independently for the server and the realm-import Job
- [[operator-rolling-updates]] — How the RHBK Operator decides between a zero-downtime rolling update and a downtime-incurring recreate when the [[keycloak-cr]] changes
- [[operator-scheduling]] — The `spec.scheduling` stanza on the [[keycloak-cr]] — standard Kubernetes placement controls for the server Pods
- [[operator-secret-references]] — How the [[keycloak-cr]] points at Kubernetes Secrets/ConfigMaps, and how the Operator reacts when their contents change
- [[oracle-jdbc-failover]] — Symptom → cause → fix for RHBK failing to fail over between Oracle SCAN/`ADDRESS` hosts over TLS at startup.
- [[otp-policies]] — Realm-level policy (Authentication → Policy → OTP Policy) that configures the one-time-password generator (FreeOTP / Google Authenticator) used as a second factor, choosing between time-based (TOTP) and counter-based (HOTP) algorithms.
- [[override-built-in-providers]] — To replace a built-in RHBK provider whose id must stay fixed, implement a factory with the same provider id and a higher `order()` — the highest-order implementation for a given id wins at runtime.
- [[password-policies]] — Realm-level rules (Authentication → Policies) constraining password strength, age, reuse, and the hashing algorithm; new realms have no policy, so set one before creating users.
- [[permission-ticket]] — A permission ticket is an opaque UMA token, issued only by resource servers via the [[protection-api|Permission Endpoint]], that names the resources/scopes a client is requesting plus the access context. The client exchanges it at the token endpoint for an [[requesting-party-token|RPT]].
- [[persistent-sessions-db-cleanup]] — Symptom → cause → fix for `user_session` rows surviving logout/expiry under RHBK 26 persistent sessions.
- [[pkce]] — PKCE binds an authorization code to the browser session that requested it by requiring the client to prove knowledge of a per-request secret at the token endpoint, blocking code interception and injection attacks.
- [[policy-enforcement-mode]] — A resource-server setting that decides the outcome when a requested resource has no associated [[authorization-permissions|permission/policy]] — the default-deny vs default-allow knob.
- [[policy-enforcer]] — A policy enforcer is the Policy Enforcement Point at the resource server: it talks to RHBK Authorization Services, obtains authorization data, and gates access to protected resources based on the [[requesting-party-token|RPT]] decisions. RHBK ships Java and JavaScript enforcers.
- [[policy-evaluation-tool]] — An Admin Console tester (the Evaluate tab on a resource server) that simulates authorization requests so you can see how your [[authorization-policy-types|policies]] resolve before deploying a [[policy-enforcer]].
- [[protection-api]] — The Protection API is a UMA-compliant set of endpoints that lets a resource server remotely manage its own resources, permission tickets, and (per-user) policies. Access requires a special access token — the PAT — carrying the `uma_protection` scope.
- [[quarkus-config-migration]] — RHBK replaces RH-SSO 7.6's JBoss EAP runtime with Quarkus, so `standalone.xml`, `jboss-cli`, and the operating-mode start scripts no longer apply — everything is now a flat `kc.sh` configuration option
- [[realm-import-export]] — Mechanisms for moving realm configuration into RHBK — partial import of an exported realm JSON via the Admin Console / `partialImport` endpoint, versus boot-time export/import for full backups.
- [[realm-keys-and-rotation]] — Each realm uses asymmetric key pairs (one active, optionally several passive) supplied by key providers to sign and sometimes encrypt the tokens, cookies, and SAML documents that protocols require; keys are rotated by adding a higher-priority provider and later removing the old one.
- [[realm-resource-access]] — Each realm has a dedicated Admin Console; administrative authority is granted through client roles of the `realm-management` client (per-realm) and `admin`/`master` roles in the master realm (cross-realm).
- [[redirect-uri-validation]] — The authorization server must match every redirect_uri against pre-registered values using exact string comparison; any looser matching (prefix, wildcard, regex) creates an open-redirect that allows auth-code or token exfiltration to an attacker-controlled URI.
- [[refresh-token-rotation]] — Refresh token rotation is a one-time-use mechanism that defends public clients against stolen refresh tokens by issuing a new token on every refresh and revoking the entire grant chain when a replayed (already-consumed) token is detected.
- [[requesting-party-token]] — An RPT is an access token issued by the token endpoint that carries the permissions RHBK granted after evaluating all policies for the requested resources/scopes. Clients request it with the UMA-ticket grant and present it as a bearer token to the resource server.
- [[reverse-proxy-configuration]] — Behind a proxy/load balancer, RHBK must be told which forwarding headers to trust via `proxy-headers`; misconfiguration causes 403s or opens spoofing vulnerabilities.
- [[rhbk-db-connection-pool]] — For HA deployments the guide recommends setting the initial, minimum and maximum JDBC pool sizes equal so connections are pre-created and kept open, preserving server-side statement caching
- [[rhbk-operator]] — The OpenShift/Kubernetes operator that deploys and manages RHBK declaratively via the `Keycloak` custom resource, installed through OLM
- [[roles-and-groups]] — Roles (realm roles and client roles, optionally composite) grant permissions; groups bundle role assignments and attributes for sets of users — both are managed in the Admin Console or via `kcadm.sh`.
- [[rp-initiated-logout]] — RP-Initiated Logout is the OIDC flow where the client redirects the user agent to the OP's end_session_endpoint to terminate the session; several validation rules around id_token_hint, post_logout_redirect_uri, and state make it a common source of open-redirect and DoS vulnerabilities.
- [[separate-sso-admin-hostnames]] — Symptom → cause → fix for serving distinct user (sso) and admin hostnames behind a proxy without a canonical redirect.
- [[server-metrics-endpoint]] — With `metrics-enabled=true`, RHBK exposes a Prometheus/OpenMetrics `/metrics` endpoint on the management port serving System, JVM, Database, HTTP, and Infinispan Cache metric families.
- [[service-level-indicators]] — RHBK ships example availability, latency, and error SLIs/SLOs computed from `up` and `http_server_requests_seconds_*` with PromQL, to track the login/refresh/logout user journey against targets.
- [[service-to-service-client-credentials]] — The OAuth 2.0 Client Credentials grant (grant_type=client_credentials) is the correct mechanism for machine-to-machine authentication where no resource-owner is present; the client authenticates as itself using strong credentials and receives a short-lived access token with no refresh token.
- [[session-persistence-volatile]] — By default RHBK persists user sessions in the database and caches them in Infinispan; the cache can instead be made the source of truth to cut DB load, at the cost of losing all sessions if every node restarts
- [[site-synchronization]] — The manual procedure to re-synchronize two Data Grid sites after their caches diverge — e.g. after a split-brain or a maintenance outage. The offline site's caches are cleared and replaced from the active site
- [[state-and-nonce]] — OAuth `state` prevents login CSRF by binding the authorization callback to the originating browser session; OIDC `nonce` additionally binds the returned ID Token to that same session, blocking replay and cross-session injection. Both must be validated on the client — omitting either leaves a distinct, exploitable gap.
- [[step-up-authentication]] — A flow pattern that grants access to clients or resources based on the user's achieved authentication assurance level, prompting an additional factor only when a higher level is required.
- [[tf-authentication-flows]] — The `keycloak/keycloak` Terraform resources that declaratively build authentication flows — the flow container, its nested subflows, the individual executions, per-execution config, and the realm bindings that activate a flow
- [[tf-client-authorization]] — resources** + **scopes** (what is protected and which actions), **policies
- [[tf-data-sources]] — Terraform `data` blocks in the `keycloak/keycloak` provider that *read* existing Keycloak/RHBK objects (realms, clients, roles, users, groups, scopes, auth flows, keys) so you can reference their IDs and attributes without managing their lifecycle.
- [[tf-identity-providers]] — The `keycloak/keycloak` Terraform resources for declaring external identity providers (OIDC, social, SAML, Kubernetes, SPIFFE) and the IdP mappers that transform brokered identities into Keycloak users, roles, groups and attributes
- [[tf-ldap-federation]] — The `keycloak/keycloak` Terraform resources that declare an LDAP/AD user-federation provider (or a custom User Storage SPI provider) plus the mappers that translate LDAP attributes, groups and roles into the Keycloak user model
- [[tf-openid-client]] — The core `keycloak/keycloak` Terraform resources for declaring OIDC clients, their client scopes (default/optional), scope-based permissions, and service-account role assignments
- [[tf-protocol-mappers]] — The `keycloak/keycloak` Terraform resources that declare *protocol mappers* — the rules that shape what claims/attributes land in OIDC tokens (access / ID / UserInfo / introspection) and SAML assertions — declaratively as code, attached to a client or a shared client scope
- [[tf-realm-resources]] — The `keycloak/keycloak` Terraform resources that create a realm and everything configured at the realm level — events, localization, user profile, client-scope defaults, client policies, signing keystores, default groups/roles, required actions, organizations, and workflows
- [[tf-roles-groups-users]] — The assignment resources are where the \"authoritative vs partial\" model matters.
- [[tf-saml-clients]] — The `keycloak/keycloak` Terraform resources that declaratively manage SAML-protocol clients, their client scopes, and the default-scope attachments in a Keycloak/RHBK realm
- [[tls-configuration]] — RHBK serves HTTPS on port 8443 using either PEM cert/key files or a Java keystore; production must never expose plain HTTP endpoints.
- [[token-exchange]] — Using one token (or credentials) to obtain a different token, via the `urn:ietf:params:oauth:grant-type:token-exchange` grant on the realm token endpoint
- [[token-introspection]] — RFC 7662 defines a protected POST endpoint that lets a resource server ask the authorization server whether a token is currently active; misuse of the `active` flag or weak caching is the primary cause of revoked tokens remaining valid.
- [[token-revocation]] — RFC 7009 defines how clients signal an authorization server to invalidate a token immediately; correct implementation requires HTTPS-only endpoints, client-authenticated requests, and cascade invalidation of all tokens from the same grant.
- [[token-storage-browser]] — No safe purely-browser token store exists: localStorage/sessionStorage are readable by any JS (including XSS payloads), in-memory tokens vanish on reload, and Web Workers do not sandbox token data from injected scripts. The only architecture that removes tokens from the browser entirely is the BFF / Token Handler pattern using HttpOnly encrypted cookies.
- [[tracing-otlp]] — RHBK records OpenTelemetry spans for the request lifecycle via a supported Quarkus OTel extension, exporting in batches over gRPC OTLP to `http://localhost:4317` by default, with configurable sampling to control volume.
- [[uneven-pod-load-master-realm]] — Symptom → cause → fix for one pod hot, all pods flooding LOGIN_ERROR/user_not_found, near-zero throughput.
- [[user-storage-spi]] — The SPI for bridging an external proprietary user/credential store into RHBK's user metamodel — the same SPI the built-in LDAP and Active Directory federation is implemented on top of.
- [[vault-spi]] — An SPI in the `org.keycloak.vault` package for writing a custom extension that connects RHBK to an arbitrary secrets-vault implementation; the built-in `files-plaintext` provider is the reference example.

## Questions (6)
- [[active-passive-session-consistency-failover]] — In Active/Passive multi-site RHBK 26, the synchronously-replicated database is the durable source of truth and external Data Grid Cross-DC keeps session caches consistent between sites; a site failover loses only in-flight requests (and incurs up to ~5 min downtime), but a partial replication failure breaks the no-loss guarantee until a manual re-sync.
- [[angular-spa-oidc-best-practice]] — An Angular SPA must use a public client with Authorization Code + PKCE (S256), client authentication OFF, implicit/direct-grant OFF, exact redirect URIs, and logout via end_session_endpoint; tokens should be sender-constrained with DPoP or, for the strongest XSS posture, moved server-side behind a BFF. keycloak-angular (Keycloak-committed) or angular-auth-oidc-client (provider-agnostic) are the library choices.
- [[ldap-import-vs-noimport]] — Import ON keeps a synchronized local copy (full admin listing/search, attachable local data) and suits manageable directories; Import OFF reads on demand for very large directories at the cost of some listing capability — pair with the right edit mode.
- [[rhbk-default-password-hash]] — Argon2 is the default password hashing algorithm in RHBK 26.x non-FIPS deployments (since 26.0); FIPS deployments default to PBKDF2-SHA512. Change it server-wide with --spi-password-hashing-provider-default.
- [[rhsso-to-rhbk-custom-providers-spis]] — Custom SPI providers always require a recompile+rebuild for RHBK 26 (Jakarta EE 10, consolidated KeycloakSession, removed deps, new JAR-in-providers/ deployment with kc.sh build); the SPI/factory contract and most provider logic carry over unchanged, but EAR/WAR packaging, hot-deploy, the AngularJS Admin/Account console themes, and several APIs do not.
- [[spa-resource-server-implementation-review]] — Applied instance of the SSO implementation-review lens: what a correct Angular-SPA-plus-resource-server integration against an RHBK realm looks like across both surfaces, and the concrete tickets that betray a wrong build.
