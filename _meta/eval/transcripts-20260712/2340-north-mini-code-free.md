# opencode session ses_0a7f109a0ffeLHmkv1lW4nDCyV — 2026-07-12 23:40
model: north-mini-code-free

## USER
<system-reminder>Note: The user selected #0-508 from "/Users/guylavian/Downloads/skills/llm-wiki/last-commit.diff". ```# git diff HEAD (fd039fd eval(record): commit both 2026-07-12 cohort answer sets, manifests, run logs) — uncommitted changes vs last commit, Sun Jul 12 19:16:56 IDT 2026
# untracked files (content not in diff): 49
?? _meta/tests/run300_scheduler_probe.py
?? last-commit.diff
?? questions/access-token-cryptographic-verification.md
?? questions/access-token-transmission-handling-rules.md
?? questions/ad-forest-recovery-procedure.md
?? questions/ad-integrated-dns-vs-traditional-zone-transfers.md
?? questions/air-gapped-client-apps.md
?? questions/authorization-permission-rhbk-definition.md
?? questions/back-channel-logout-oidc.md
?? questions/build-vs-runtime-options-rhbk.md
?? questions/cache-embedded-mcast-buffer-size-fabricated.md
?? questions/certificate-templates.md
?? questions/common-jwt-validation-pitfalls.md
?? questions/credential-theft-active-directory-techniques.md
?? questions/default-built-in-user-accounts-ad-domain.md
?? questions/etherchannel-load-balancing.md
?? questions/fine-grained-password-policies-explained.md
?? questions/get-adfinegrainedpasswordpolicyresult-cmdlet.md
?? questions/group-policy-central-management.md
?? questions/how-fine-grained-authorization-works.md
?? questions/kc-cache-stack-mode.md
?? questions/kc-db-pool-validation-timeout.md
?? questions/kc-db-url-password.md
?? questions/kc-events-log-verbosity.md
?? questions/kc-health-metrics-enabled-env-var.md
?? questions/kc-spi-cache-embedded-default-cluster-name.md
?? questions/keycloak-built-in-policy-types.md
?? questions/kubernetes-preferred-zone-node-label-does-not-exist.md
?? questions/microsoft-defensive-security-model-ad.md
?? questions/ms-ds-allowed-to-delegate-from-attribute.md
?? questions/openshift-route-disable-cookie-annotation.md
?? questions/ospfv2-routing-ios-xe.md
?? questions/policy-based-routing.md
?? questions/resources-scopes-rhbk-authorization.md
?? questions/rhbk-ha-cross-site-model.md
?? questions/rhbk-observability-endpoints-signals.md
?? questions/rhbk-openshift-end-to-end-path.md
?? questions/rhbk-two-ha-architectures.md
?? questions/rhsso-to-rhbk-migration-what-is-involved.md
?? questions/saml-sp-integration-rhbk-migration.md

diff --git a/.obsidian/graph.json b/.obsidian/graph.json
index 507e836..59512df 100644
--- a/.obsidian/graph.json
+++ b/.obsidian/graph.json
@@ -17,6 +17,6 @@
   "repelStrength": 10,
   "linkStrength": 1,
   "linkDistance": 250,
-  "scale": 0.06644324827659766,
+  "scale": 0.22088802605985405,
   "close": false
 }
\ No newline at end of file
diff --git a/index.active-directory.md b/index.active-directory.md
index 4e26ff6..ad65f3b 100644
--- a/index.active-directory.md
+++ b/index.active-directory.md
@@ -1,7 +1,7 @@
 # active-directory — routing index
 
 _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `active-directory` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
-**84 pages.**
+**96 pages.**
 
 
 ## Topics (14)
@@ -73,23 +73,35 @@ _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain rou
 - [[vm-generation-id-safe-restore]] — VM-GenerationID is a hypervisor-supplied GUID that changes on snapshot restore or VM copy; AD DS on Windows Server 2012+ compares it to the stored value in NTDS.DIT and, on mismatch, automatically resets the InvocationID and discards the RID pool to prevent USN rollback and duplicate SID issuance.
 - [[windows-time-service]] — The W32Time service maintains clock synchronization across the domain hierarchy, with the PDC emulator as the authoritative time source; a skew exceeding 5 minutes breaks Kerberos authentication.
 
-## Questions (19)
+## Questions (31)
 - [[active-directory-domain-services-fundamentals]] — AD DS is Microsoft's directory service — a hierarchical, multi-master-replicated store of users, computers, and policy, hard-wired to DNS for locating DCs and to Kerberos/NTLM for authentication. Its fundamental concepts are: forest/domain/OU hierarchy, multi-master replication with five FSMO single-master roles, the security principal model (users/computers/groups with SIDs), Group Policy, DNS integration, and site topology for replication efficiency.
+- [[ad-forest-recovery-procedure]] — Forest recovery is the last-resort procedure for when a forest-wide failure (corruption, ransomware, bad change replicated everywhere) renders all DCs unusable — you restore one DC per domain from a known-good backup, seize FSMO roles, reset secrets, clean up metadata, then rebuild the remaining DCs.
+- [[ad-integrated-dns-vs-traditional-zone-transfers]] — AD-integrated DNS zones store zone data in the directory and replicate it via AD replication (multi-master, no separate topology), while traditional zones use file-backed storage with a single writable primary and DNS zone transfers (AXFR/IXFR) to secondaries.
 - [[ad-multi-master-replication-convergence]] — AD DS uses a state-based replication engine — USN counters, InvocationID-scoped write tracking, the up-to-dateness vector, and high-water-mark filtering — built on a KCC-generated topology — to converge any write from any DC across the forest.
+- [[certificate-templates]] — Certificate templates are the rules/settings an Enterprise CA applies to incoming certificate requests — stored in AD DS for forest-wide consistency, only usable by Enterprise CAs, and the enforcement point for TPM key attestation.
+- [[credential-theft-active-directory-techniques]] — Attackers extract credential hashes, Kerberos tickets, or plaintext from LSASS memory after gaining SYSTEM/Admin access to a domain-joined host, then replay them laterally (pass-the-hash, pass-the-ticket) — targeting privileged domain accounts (EA/DA/BA), VIP accounts, and privilege-attached service accounts — to escalate to full forest compromise.
 - [[cross-site-split-brain-pac-signing]] — After an inter-site network partition (split-brain), two AD Domain Controllers diverged. When the network recovered, the replication delta produces Kerberos PAC signing errors because the divergent directory state causes PAC validation failures. Fix replication first — the PAC errors are a downstream symptom of inconsistent group/key state.
 - [[dc-locator-how-windows-clients-find-dc]] — Windows clients locate a domain controller via the DC Locator algorithm — DNS SRV queries under _msdcs, UDP LDAP pings, site-affinity redirect, and Netlogon cache, with NetBIOS fallback disabled by default in Windows Server 2025.
+- [[default-built-in-user-accounts-ad-domain]] — Active Directory creates three built-in user accounts automatically — Administrator (RID 500), Guest (RID 501), and KRBTGT (RID 502).
 - [[deploy-promote-domain-controllers]] — Two-phase process: install the AD DS role, then promote via Install-ADDSForest (new forest), Install-ADDSDomain (child/tree), Install-ADDSDomainController (replica DC), or staged RODC. Adprep runs automatically; site assignment, DSRM password, and DNS delegation are key prerequisites.
 - [[domain-forest-functional-levels-how-they-work]] — Domain (DFL) and forest (FFL) functional levels are the AD DS compatibility and feature gate — they control which Windows Server OS versions can run as DCs and which AD features are available.
+- [[fine-grained-password-policies-explained]] — FGPP (Password Settings Objects) let a single AD domain enforce different password and lockout rules per user/group — used whenever the org needs stricter controls for privileged accounts, compliance tiers, or service accounts without spinning up extra domains.
 - [[fsmo-seize-after-primary-dc-loss]] — When the primary-site DC holding the PDC Emulator and RID Master roles is destroyed and unrecoverable, seize both roles onto a surviving secondary DC via PowerShell or ntdsutil, then clean up the dead DC's metadata, reset time configuration, and manage the RID pool.
 - [[fsmo-transfer-vs-seize-dead-dc-twist]] — ניתוח ארבע-חלקי של FSMO transfer מול seize: ההבחנה המהותית, הסכנה בהחזרת DC מת, דחיפות שחזור תפקידים, והטוויסט — מתי דווקא אפשר להחזיר DC אחרי העברת תפקיד
+- [[get-adfinegrainedpasswordpolicyresult-cmdlet]] — Get-ADFineGrainedPasswordPolicyResult does not exist in the AD PowerShell module. The real cmdlets are Get-ADFineGrainedPasswordPolicy (returns ADFineGrainedPasswordPolicy objects), Get-ADFineGrainedPasswordPolicySubject (returns subject users/groups), and Get-ADUserResultantPasswordPolicy (returns the resolved effective policy for a user).
+- [[group-policy-central-management]] — Group Policy centralizes management through Group Policy Objects (GPOs)—a container in AD (domain partition) + a template in SYSVOL—linked to sites, domains, or OUs, with client-side extensions applying Computer Configuration at startup and User Configuration at sign-in, processed in LSDOU order.
 - [[how-active-directory-stores-directory-data]] — Active Directory stores all directory objects in an Extensible Storage Engine (ESE) database file (NTDS.dit), defaulting to an 8k page format, with a 32k page option in Windows Server 2025.
 - [[kcd-rbcd-mutual-exclusivity]] — No — classic constrained delegation (msDS-AllowedToDelegateTo, on the front-end account) and RBCD (msDS-AllowedToActOnBehalfOfOtherIdentity, a security descriptor on the resource account) are independent attributes on different objects and can coexist. Both configured on the same accounts is a documented escalation pattern, not a benign "both work" — monitor SD writes via Event 5136.
 - [[kerberos-preauth-4771-bruteforce]] — Event 4771 (failure code 0x18 = bad password) repeating against one account is the on-DC signature of a Kerberos password-guessing attempt; correlate with 4740 (lockout) and the account-lockout policy. 4771/4740 are corpus-grounded as events-to-monitor; the per-code detail comes from the windows-eventlog skill.
 - [[kerberos-rbcd-s4u-delegation-detailed]] — Deep dive into Resource-Based Constrained Delegation (RBCD): how S4U2Self+S4U2Proxy overcome NTLM front-end auth without TRUSTED_TO_AUTH_FOR_DELEGATION, why RBCD's S4U2Proxy succeeds with a non-forwardable evidence ticket when classic constrained delegation fails, and the security implications of delegating authority to the resource owner.
+- [[microsoft-defensive-security-model-ad]] — Microsoft's four-pillar defensive model for AD DS: reduce attack surface, enforce least-privilege/tiered administration, monitor for compromise, and plan for breach recovery.
+- [[ms-ds-allowed-to-delegate-from-attribute]] — ms-DS-Allowed-To-Delegate-From is not a real AD schema attribute — no AD attribute by that name exists. The closest real attributes are ms-DS-Allowed-To-Delegate-To (classic constrained delegation, on the front-end account) and ms-DS-Allowed-To-Act-On-Behalf-Of-Other-Identity (RBCD, on the resource account).
 - [[primary-ad-management-tools]] — The four primary AD DS management tools are ADAC (dsac.exe), the ActiveDirectory PowerShell module, GPMC (gpmc.msc), and the classic ADUC (dsa.msc) snap-in.
 - [[replication-error-8614-tombstone-lifetime]] — Error 8614 ("The Active Directory cannot replicate with this partner because the time since the last replication with this partner has exceeded the tombstone lifetime") indicates a DC was offline past the forest tombstone lifetime (default 180 days), creating lingering objects; resolution requires identifying and removing lingering objects, then re-establishing replication.
 - [[security-principals-definition]] — A security principal is any entity the OS can authenticate — user, computer, or group — each represented by a unique SID, forming the foundation of Windows access control.
 - [[sysvol-folder-function]] — SYSVOL is the shared system volume on every domain controller that hosts the Group Policy template files, logon/logoff/startup/shutdown scripts, and other domain-wide file-based policy content, replicated between DCs via FRS or DFSR.
+- [[types-of-service-accounts]] — AD DS provides four service-account types — sMSA, gMSA, dMSA, and virtual accounts — each with different password management, SPN handling, and multi-server support characteristics.
 - [[virtualized-domain-controllers-safety]] — Windows Server 2012+ DCs use VM-GenerationID (a hypervisor GUID that changes on snapshot restore) to detect rollbacks and automatically reset the InvocationID, discard the RID pool, and non-authoritatively sync SYSVOL — preventing USN rollback and replication divergence.
 - [[w32tm-resync-force-flag]] — No — `/force` is not a documented switch of `w32tm /resync` (the real ones are /computer, /nowait, /rediscover, /soft); the correct PDC-emulator sequence is `w32tm /config … /update` then `w32tm /resync` (add /rediscover if the peer list changed). The myth likely migrates from `nltest /dsgetdc … /force`, which does exist.
+- [[why-dns-essential-ad-ds]] — AD DS uses DNS as its mandatory locator service — clients find domain controllers, and DCs find each other, through DNS SRV records under `_msdcs`. Without working DNS, AD is unreachable.
 - [[windows-server-identity-coverage-gaps]] — Gap analysis of the active-directory wiki brain against the full Microsoft Learn "Windows Server identity" doc set (4,163-page PDF). Of the in-scope pillars, Windows LAPS, AD CS, and the Solutions/Scenario guides are absent from the vault corpus entirely (AD FS is deliberately out of scope); within the one pillar present (AD DS) only FSMO has a synthesis page — ~221 reference notes are un-synthesized.
diff --git a/index.cisco-ios-xe.md b/index.cisco-ios-xe.md
index be2493a..12b72b9 100644
--- a/index.cisco-ios-xe.md
+++ b/index.cisco-ios-xe.md
@@ -1,7 +1,7 @@
 # cisco-ios-xe — routing index
 
 _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `cisco-ios-xe` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
-**22 pages.**
+**25 pages.**
 
 
 ## Topics (2)
@@ -21,13 +21,16 @@ _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain rou
 - [[static-and-default-routes]] — Static routes (default AD 1), floating statics, and the three ways to set a gateway of last resort — plus the two traps: interface-pointing statics get advertised like connected routes, and a 0.0.0.0/0 static won't propagate into OSPF/IS-IS.
 - [[vlans-and-trunking]] — 802.1Q vs ISL VLAN encapsulation on router subinterfaces for inter-VLAN routing, and the native-VLAN/PVID rule whose mismatch silently merges traffic.
 
-## Questions (9)
+## Questions (12)
 - [[bgp-4-ios-xe]] — BGP-4 (Border Gateway Protocol version 4) is the interdomain path-vector routing protocol that exchanges loop-free reachability between autonomous systems over TCP port 179. On IOS XE, it installs one best path per prefix, uses AS_Path for loop prevention, supports 2-byte and 4-byte ASNs (asplain/asdot), and requires the next hop to be reachable (usually via the IGP) for any route to be usable.
 - [[c9500-issu-svl-blackhole-nsf-gr]] — On a Catalyst 9500 StackWise Virtual pair, ISSU caused ~90-second traffic blackhole and OSPF/BGP flapping despite the standby reloading and rejoining cleanly. The root cause is missing OSPF NSF and BGP Graceful Restart configuration, not an ISSU procedure error — the routing protocols tore down adjacencies during the control-plane switchover because no grace signal was sent.
 - [[cisco-ios-xe-ospfneighbordown]] — Post-mortem of an OSPF adjacency stuck in EXSTART between dist-rtr-1 Gi0/1 (MTU 9000, jumbo) and core-1 Gi0/2 (MTU 1500). The DBD interface-MTU field check on the smaller-MTU side rejects the peer's DBD, so the pair never advances to FULL; fix is to match interface MTU (or apply ip ospf mtu-ignore).
 - [[distance-vrf-aware]] — The command 'distance vrf-aware' does not exist in the Cisco IOS-XE codified corpus — it is a fabricated command.
+- [[etherchannel-load-balancing]] — EtherChannel bonds multiple physical links into one logical port-channel for bandwidth+redundancy; load balancing uses a 16-bucket flow-based hash per port-channel, with per-port-channel override of the global method.
 - [[lacp-fast-switchover-prereqs]] — lacp fast-switchover enables EtherChannel flow-based limited 1:1 redundancy — one active + one hot-standby member with fast failover instead of full reconvergence. It must be enabled at BOTH ends, requires lacp max-bundle 1 on a two-port LACP channel; port-priority choice controls whether the channel reverts to the primary link.
 - [[mixed-speed-port-channel]] — Mixing a 1G and a 10G link in the same LACP port-channel causes out-of-order packets and throughput degradation because (a) Cisco does not support mixed-speed EtherChannel members, and (b) flow-based load balancing distributes packets across links with ~10× serialization-delay difference, causing reordering at the receiver.
+- [[ospfv2-routing-ios-xe]] — OSPFv2 (RFC 2328) on IOS XE is a link-state IGP that floods LSAs within areas, runs SPF to build a loop-free shortest-path tree, uses cost (derived from bandwidth) for path selection, elects DR/BDR on multiaccess networks, and requires consistent hello/dead timers, authentication, and MTU for adjacency formation.
+- [[policy-based-routing]] — Policy-based routing (PBR) forwards packets by a route map applied to an interface instead of by destination-based longest-prefix lookup — used for source-sensitive routing, protocol-sensitive routing, equal-access, or dedicated-link steering.
 - [[redistribute-ospf-into-ebgp]] — Redistribute OSPFv2 routes into an eBGP process on Cisco IOS XE — the `redistribute ospf` command under `router bgp`, controlling which routes are injected with a route map, setting the MED, and the key gotchas (metric translation, eBGP next-hop reachability, static default caveat).
 - [[static-routes-default-routes-ios-xe]] — Static routes are user-defined paths with AD 1; default routes set the gateway of last resort via one of three mechanisms. Key traps: interface-pointing statics behave as connected, and 0.0.0.0/0 statics don't propagate into OSPF/IS-IS.
 - [[vlan-trunking-ios-xe]] — How 802.1Q and ISL frame tagging work on IOS XE router subinterfaces for inter-VLAN routing, and the native-VLAN/PVID mismatch pitfall.
diff --git a/index.keycloak.md b/index.keycloak.md
index 595c74f..4599af3 100644
--- a/index.keycloak.md
+++ b/index.keycloak.md
@@ -1,7 +1,7 @@
 # keycloak — routing index
 
 _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `keycloak` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
-**207 pages.**
+**227 pages.**
 
 
 ## Topics (27)
@@ -161,26 +161,36 @@ _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain rou
 - [[user-storage-spi]] — The SPI for bridging an external proprietary user/credential store into RHBK's user metamodel — the same SPI the built-in LDAP and Active Directory federation is implemented on top of.
 - [[vault-spi]] — An SPI in the `org.keycloak.vault` package for writing a custom extension that connects RHBK to an arbitrary secrets-vault implementation; the built-in `files-plaintext` provider is the reference example.
 
-## Questions (54)
+## Questions (74)
+- [[access-token-cryptographic-verification]] — A resource server must verify signature, typ, iss, aud, exp, required claims, scope, and derive identity from the token — in that order — before granting access.
 - [[active-active-invalid-grant-token-refresh]] — Running RHBK 26 in a two-site Active-Active topology with a shared external Data Grid and round-robin + sticky sessions produces intermittent 'invalid_grant' on token refresh — not primarily a stickiness, replication-lag, or clock problem, but an architecture mismatch: the documented model is Active/Passive, and the Active-Active round-robin causes session-cache churn across sites (local caches are cold for sessions from the other site, triggering DB round-trips under load), plus potential 'work' cache deadlocks (explicitly warned against in the guide for active-active setups). Sticky sessions help but cannot eliminate cross-site routing under load, and a single shared Data Grid cluster (instead of two with Cross-DC) adds latency and split-brain risk.
 - [[active-passive-failover-sessions-lost]] — In an RHBK Active-Passive multi-site setup with external Data Grid, sessions lost on failover means one of the critical durability layers is missing or misconfigured: the `sessions` cache is NOT cross-site replicated, so session survival depends entirely on DB persistence + synchronous DB replication. The most common causes are (1) `multi-site` feature flag not enabled, (2) volatile sessions (`persistent-user-sessions` disabled), (3) database not synchronously replicated, or (4) a single Data Grid cluster instead of two with Cross-DC.
 - [[active-passive-session-consistency-failover]] — In Active/Passive multi-site RHBK 26, the synchronously-replicated database is the durable source of truth and external Data Grid Cross-DC keeps session caches consistent between sites; a site failover loses only in-flight requests (and incurs up to ~5 min downtime), but a partial replication failure breaks the no-loss guarantee until a manual re-sync.
 - [[angular-spa-oidc-best-practice]] — An Angular SPA must use a public client with Authorization Code + PKCE (S256), client authentication OFF, implicit/direct-grant OFF, exact redirect URIs, and logout via end_session_endpoint; tokens should be sender-constrained with DPoP or, for the strongest XSS posture, moved server-side behind a BFF. keycloak-angular (Keycloak-committed) or angular-auth-oidc-client (provider-agnostic) are the library choices.
 - [[authentication-flow-definition]] — An authentication flow is an ordered container of executions (authenticators/screens/actions) and sub-flows with requirement types that determines how a user is authenticated during login, registration, password reset, and brokering.
 - [[bff-token-theft-spa]] — A Backend-for-Frontend eliminates XSS-based token theft by running the OAuth flow server-side, storing tokens on the server, and exposing only encrypted HttpOnly cookies to the browser.
+- [[cache-embedded-mcast-buffer-size-fabricated]] — `--cache-embedded-mcast-buffer-size` is a fabricated flag — it does not appear in any RHBK/Keycloak reference document. The closest real multicast configuration is `-Djgroups.mcast_port` passed via `JAVA_OPTS_APPEND`.
 - [[client-credentials-burst-token-expired]] — Under burst load (thousands of parallel client_credentials token requests at cache expiry), tokens issued seconds ago are intermittently rejected by the resource server as expired — with clock skew ruled out — the likely root cause is a resource-server JWKS/introspection cache failure exacerbated by key rotation or clock skew at the RS, not a Keycloak issuance defect or a lifespan-too-short problem.
+- [[common-jwt-validation-pitfalls]] — A catalogue of the ways JWT validation goes wrong — algorithm confusion, skipped signature checks, header-injection, and cross-token misuse.
 - [[consistency-over-availability-two-site]] — Red Hat's HA guide explicitly prioritizes consistency over availability to prevent stale data (old passwords, invalid sessions) during failures. For a two-site Active/Passive setup this means synchronous DB + Data Grid replication, a FAIL failure policy that errors rather than serves stale data, low-latency requirement, fencing to resolve split-brain, and mandatory manual re-sync after any partition.
 - [[fapi-20-security-profile-mandates-high-value-apis]] — FAPI 2.0 (OIDF final) mandates code-flow-only with PAR, PKCE S256, mTLS or private_key_jwt client auth, sender-constrained tokens (mTLS/DPoP), the iss response parameter, and TLS 1.2+ — enforced in RHBK via the built-in fapi-2-* client profiles.
 - [[feature-flags-enable-disable]] — Optional/preview/deprecated features are toggled with the build-time `features`/`features-disabled` options; changes require `kc.sh build`.
 - [[four-ordered-config-sources]] — RHBK reads options from four sources in descending priority: CLI flags, env vars, config file, KeyStore.
 - [[hostname-admin-strict-env-var]] — KC_HOSTNAME_ADMIN_STRICT does not exist in the RHBK/Keycloak corpus; the real options are KC_HOSTNAME_ADMIN (admin hostname) and --hostname-strict / KC_HOSTNAME_STRICT (global strict mode).
 - [[how-does-rhbk-issue-scope-and-expire-tokens-and-sessions]] — RHBK issues tokens via OIDC/OAuth2 grant types on the token endpoint; scopes them with the scope parameter, client scopes, protocol mappers, and client policies; and expires them with layered session/token lifespans (SSO Session, Client Session, Offline Session, Access Token, Refresh Token) configured per-realm and per-client.
+- [[how-fine-grained-authorization-works]] — RHBK Authorization Services implement a XACML-style PAP/PDP/PEP/PIP architecture layered on OAuth2 and UMA 2.0: you enable a confidential client as a resource server, register [[authorization-resources-scopes|resources and scopes]] (what to protect), write reusable [[authorization-policy-types|policies]] (the conditions), bind them via [[authorization-permissions|permissions]] (who can do what), and enforce with a [[policy-enforcer]] (PEP) that requests an [[requesting-party-token|RPT]] carrying the granted permissions.
 - [[identity-brokering-external-oidc-idp]] — RHBK acts as an identity broker — it delegates authentication to an external OpenID Connect provider, validates the response, imports/links the user, and then issues its own token to the client application. The client never interacts with the external IdP directly.
 - [[kb5014754-adfs-rhbk-claim-mapping]] — KB-grounded analysis of intermittent claim mapping failure after KB5014754 enforcement when RHBK does OIDC federation against AD FS. The KB cannot determine the root cause because KB5014754 and strong certificate mapping are absent from the corpus and AD FS is out of scope — but the closest analogous pattern is the mutable-AD-attribute link-loss documented in kb:7128299.
 - [[kc-20260625-active-credential-stuffing-attack-on-keycloak-to]] — Post-mortem — Active credential stuffing attack targeting POST /realms/{realm}/protocol/{protocol}/token on keycloak:9000. An external
 - [[kc-cache-embedded-mcast-port-behavior]] — KC_CACHE_EMBEDDED_MCAST_PORT is not a recognized Keycloak env var. It is silently ignored under any cache mode (including --cache=local) — no deprecation warning, no error.
+- [[kc-cache-stack-mode]] — KC_CACHE_STACK_MODE does not exist in the RHBK/Keycloak corpus. The nearest real option is KC_CACHE (--cache), which selects ispn vs local cache mode.
+- [[kc-db-pool-validation-timeout]] — KC_DB_POOL_VALIDATION_TIMEOUT is not a recognized RHBK/Keycloak environment variable; the real db-pool options are db-pool-initial-size, db-pool-min-size, db-pool-max-size, and db-pool-max-lifetime.
+- [[kc-db-url-password]] — KC_DB_URL_PASSWORD does not exist in Keycloak/RHBK. The relevant env vars are KC_DB_PASSWORD (database password) and KC_DB_URL (JDBC URL) — separate settings.
+- [[kc-events-log-verbosity]] — KC_EVENTS_LOG_VERBOSITY is not a recognized Keycloak/RHBK environment variable. The real mechanism for category-level log control is KC_LOG_LEVEL_<CATEGORY>; events storage/export is configured via SPI options, not a verbosity env var.
 - [[kc-fips-mode-strict]] — --fips-mode=strict enables BCFIPS approved mode (tighter crypto constraints); without it BCFIPS runs in non-approved mode. Mandates BCFKS keystores, ≥2048-bit RSA, ≥14-char passwords, blocks PKCS12/JKS and RSA1_5 JWE.
+- [[kc-health-metrics-enabled-env-var]] — KC_HEALTH_METRICS_ENABLED does not exist in the RHBK/Keycloak corpus. The real separate build-time options are KC_HEALTH_ENABLED (health endpoints) and KC_METRICS_ENABLED (metrics endpoint).
 - [[kc-hostname-strict-backchannel]] — The Hostname v1 option that forced backchannel URLs to use the fixed frontend hostname; removed in v2 and replaced by `hostname-backchannel-dynamic`.
+- [[kc-spi-cache-embedded-default-cluster-name]] — The environment variable KC_SPI_CACHE_EMBEDDED_DEFAULT_CLUSTER_NAME does not exist in Keycloak/RHBK; the real topology env vars are SITE_NAME, RACK_NAME, and MACHINE_NAME
 - [[keycloak-keycloakhighauthfailurerate]] — Post-mortem — The Infinispan `keys` cache was flushed (0 entries, 0 stores, 0 hits, 0 misses over a 57-minute stats window) while the
 - [[keycloak-keycloakhighservererrorrate]] — Post-mortem — The backing PostgreSQL database began returning extremely slow queries (p99 = 9.52 s, ~100× above healthy baseline). Slo
 - [[ldap-import-vs-noimport]] — Import ON keeps a synchronized local copy (full admin listing/search, attachable local data) and suits manageable directories; Import OFF reads on demand for very large directories at the cost of some listing capability — pair with the right edit mode.
@@ -201,18 +211,28 @@ _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain rou
 - [[rhbk-as-identity-broker]] — RHBK acts as an intermediary between client applications and external identity providers (social, OIDC, SAML). It delegates authentication to the external IdP, validates the response, imports/links the user, and issues its own token — so clients never interact with the external IdP directly.
 - [[rhbk-customize-user-facing-pages]] — RHBK provides two theme systems — classic Freemarker templates (Login, Email, Welcome pages) and React-based npm packages (Admin Console, Account Console) — extensible via `theme.properties`, override templates, message bundles, and the Theme Selector/Theme Resource SPIs.
 - [[rhbk-default-password-hash]] — Argon2 is the default password hashing algorithm in RHBK 26.x non-FIPS deployments (since 26.0); FIPS deployments default to PBKDF2-SHA512. Change it server-wide with --spi-password-hashing-provider-default.
+- [[rhbk-ha-cross-site-model]] — RHBK's documented cross-site HA model is a two-site Active/Passive topology using external Red Hat Data Grid clusters with Cross-DC synchronous replication, a synchronously replicated database, and an external load balancer that health-checks the /lb-check endpoint.
 - [[rhbk-ispn000541-dns-query-openshift]] — ISPN000541 / 'dns_query can not be null or empty' on OpenShift — almost always from kc.sh bootstrap-admin, not runtime. Fix: use --cache=local for the bootstrap run.
 - [[rhbk-ldap-ad-user-federation]] — RHBK ships a built-in LDAP/AD provider under the User Storage SPI that turns directory users into RHBK users, governed by storage mode (import vs on-demand), edit mode (READ_ONLY/WRITABLE/UNSYNCED), and LDAP mappers for attributes/groups/roles.
+- [[rhbk-observability-endpoints-signals]] — RHBK exposes health probes, Prometheus/OpenMetrics metrics, and OpenTelemetry traces (plus preview OTel logs/metrics) — most served on the dedicated management port 9000.
+- [[rhbk-openshift-end-to-end-path]] — The end-to-end path: install the RHBK Operator via OLM, provision DB/hostname/TLS prerequisites yourself, declare a Keycloak CR that the Operator reconciles into a StatefulSet+Service+Ingress, verify via status conditions, and access the initial admin credentials
 - [[rhbk-oscp-scaling-resources]] — Best-practice resource sizing for RHBK pods (CPU/memory), replica count/HPA, PostgreSQL connection pool, and database instance sizing on OpenShift, based on the RHBK High Availability Guide and Operator documentation.
+- [[rhbk-two-ha-architectures]] — RHBK ships two documented HA shapes: a single-cluster deployment with embedded Infinispan and a two-site Active/Passive multi-cluster deployment with external Red Hat Data Grid.
 - [[rhbk-upgrade-cadence]] — Red Hat Build of Keycloak (RHBK) משחרר מינור גרסה (~26.0, 26.2, 26.4, 26.6) בערך כל רבעון, ופטש'ים (z-stream) בתדירות משתנה לפי CVEs. ההמלצה: להישאר על הגרסה המינורית האחרונה, להחיל פטש'ים מיד כשיוצאים, ולשדרג מינור בהמתנה הבאה עם חלון downtime מתוכנן.
 - [[rhsso-openshift3-connection-problems]] — Common causes of connection failures between Red Hat Single Sign-On (RH-SSO) or Red Hat Build of Keycloak (RHBK) and OpenShift Container Platform 3.x — TLS/certificate issues, master-config misconfiguration, template version incompatibility, and product lifecycle blocks
 - [[rhsso-to-rhbk-custom-providers-spis]] — Custom SPI providers always require a recompile+rebuild for RHBK 26 (Jakarta EE 10, consolidated KeycloakSession, removed deps, new JAR-in-providers/ deployment with kc.sh build); the SPI/factory contract and most provider logic carry over unchanged, but EAR/WAR packaging, hot-deploy, the AngularJS Admin/Account console themes, and several APIs do not.
+- [[rhsso-to-rhbk-migration-what-is-involved]] — Migration from RH-SSO 7.6 (JBoss EAP-based) to RHBK (Quarkus-based) spans six surfaces: server config, database, Operator/OpenShift, application adapters, custom providers, and custom themes.
+- [[saml-sp-integration-rhbk-migration]] — SAML SPs integrate with RHBK as a SAML 2.0 IdP via entity descriptor exchange, metadata files, or protocol bindings; on RH-SSO 7.6→RHBK migration: the EAP 8 SAML feature pack and mod_auth_mellon survive, but RH-SSO 7.6 SAML adapters for EAP 6.x/7.x do not
 - [[securing-an-app-with-oidc-saml]] — Register the app as a *client* in an RHBK realm and have it speak OpenID Connect (preferred for new apps) or SAML 2.0, using the framework's native OIDC/SAML library — not a Keycloak adapter.
 - [[spa-resource-server-implementation-review]] — Applied instance of the SSO implementation-review lens: what a correct Angular-SPA-plus-resource-server integration against an RHBK realm looks like across both surfaces, and the concrete tickets that betray a wrong build.
 - [[spi-customization-rhbk]] — RHBK is customized by implementing SPIs: write a `Provider` + `ProviderFactory`, register via `META-INF/services/`, JAR goes in `providers/`, then `kc.sh build`. Two provider-type kinds (single vs multiple implementations), plus JavaScript script providers (Technology Preview) for lighter customization.
+- [[sso-implementation-review-framework]] — The SSO Implementation Review Framework is a wiki-hosted evaluation-lens (MOC page `sso-implementation-review`) that maps every OIDC/SSO security concept to a best-practice rule, its anti-pattern, and the observable symptom — providing both proactive checklists (client/SPA + backend) and a symptom-to-cause reverse index for fault diagnosis.
 - [[standalone-xml-to-kc-sh-mapping]] — How each JBoss EAP standalone.xml subsystem (datasource, TLS, cache, hostname, truststore, vault, JVM) maps to the equivalent RHBK kc.sh --option.
+- [[standalone-xml-to-kcsh-mapping]] — Each RH-SSO 7.6 standalone.xml subsystem (datasource, TLS, cache, hostname, truststore, vault, JVM, SPI) maps to flat --key=value or KC_ENV_VAR options on kc.sh, because RHBK replaces JBoss EAP with Quarkus.
 - [[temporary-admin-user-rhbk-first-start-recovery]] — RHBK provides startup flags (first start only), a dedicated `bootstrap-admin` command (first start or recovery), and Operator auto-generation to mint a temporary admin account.
 - [[terraform-keycloak-realms-declarative]] — The `keycloak/keycloak` Terraform provider manages Keycloak/RHBK realms as code — the `keycloak_realm` resource defines the realm, and ~15 realm-scoped resources cover events, user profile, keystores, client policies, default scopes, required actions, organizations, and workflows.
 - [[token-exchange-rhbk-detailed]] — Deep dive into RHBK 26.2+ Standard Token Exchange V2: why it implements impersonation (not delegation), how audience and scope combine from the requester-client's perspective (the #1 trap), four operational pitfalls (DPoP-bound subject_token, revocation chain, multi-site token lifespan, public client ban), and which use cases still require legacy V1 + FGAP:v1.
+- [[transaction-xa-retry-count]] — KC_TRANSACTION_XA_RETRY_COUNT does not exist in the Keycloak/RHBK corpus; the real XA-transaction option is KC_TRANSACTION_XA_ENABLED (build-time, enables or disables XA datasources).
+- [[users-losing-ad-federated-link]] — Root cause and fix for users sporadically losing their Active Directory identity-provider link in RHBK and being prompted to merge accounts.
 - [[what-do-ldap-mappers-do]] — LDAP mappers are components on an LDAP provider that map LDAP directory data (attributes, groups, roles) onto RHBK user attributes, group memberships, and role mappings.
 - [[what-is-keycloak]] — Keycloak (productized as Red Hat build of Keycloak) is a standalone single sign-on server that secures web apps and REST services over OIDC/OAuth2/SAML: apps redirect users to it, never see credentials, and receive signed tokens; it adds LDAP/AD federation, identity brokering, fine-grained authorization, and SPI-based extensibility, running on Quarkus with an OpenShift operator.
diff --git a/index.md b/index.md
index 2fbafc5..916b0a5 100644
--- a/index.md
+++ b/index.md
@@ -18,10 +18,11 @@ _A native [[wiki.base]] view — it reads page frontmatter directly and updates
 ## Domains
 _Per-domain routing indexes (generated). A QUERY routes here first, then opens the relevant index below to stay inside the model's context window._
 
-- [active-directory](index.active-directory.md) — 84 pages · [[_ref-active-directory|221 reference notes]] · review lens [[active-directory-implementation-review]]
-- [cisco-ios-xe](index.cisco-ios-xe.md) — 22 pages · [[_ref-cisco-ios-xe|167 reference notes]] · review lens [[cisco-ios-xe-implementation-review]]
-- [keycloak](index.keycloak.md) — 207 pages · [[_ref-keycloak|800 reference notes]] · review lens [[sso-implementation-review]]
-- [openshift](index.openshift.md) — 43 pages · [[_ref-openshift|3813 reference notes]] · review lens [[openshift-implementation-review]]
+- [active-directory](index.active-directory.md) — 96 pages · [[_ref-active-directory|221 reference notes]] · review lens [[active-directory-implementation-review]]
+- [cisco-ios-xe](index.cisco-ios-xe.md) — 25 pages · [[_ref-cisco-ios-xe|167 reference notes]] · review lens [[cisco-ios-xe-implementation-review]]
+- [keycloak](index.keycloak.md) — 227 pages · [[_ref-keycloak|800 reference notes]] · review lens [[sso-implementation-review]]
+- [openshift](index.openshift.md) — 44 pages · [[_ref-openshift|3813 reference notes]] · review lens [[openshift-implementation-review]]
+- [unfiled](index.unfiled.md) — 1 pages
 <!-- index.py:domains:end -->
 
 ## Topics
@@ -233,7 +234,10 @@ _Per-domain routing indexes (generated). A QUERY routes here first, then opens t
 - [questions/rhbk-customize-user-facing-pages.md](./questions/rhbk-customize-user-facing-pages.md)
 - [questions/what-do-ldap-mappers-do.md](./questions/what-do-ldap-mappers-do.md)
 - [questions/openshift-persistent-storage-pvc-dynamic-provisioning.md](./questions/openshift-persistent-storage-pvc-dynamic-provisioning.md)
+- [questions/openshift-route-disable-cookie-annotation.md](./questions/openshift-route-disable-cookie-annotation.md)
 - [questions/what-is-keycloak.md](./questions/what-is-keycloak.md)
+- [questions/kc-db-url-password.md](./questions/kc-db-url-password.md)
+- [questions/ms-ds-allowed-to-delegate-from-attribute.md](./questions/ms-ds-allowed-to-delegate-from-attribute.md)
 
 ---
 _Seeded 2026-06-16. Grow it via the INGEST / QUERY operations in `CLAUDE.md`.
diff --git a/index.openshift.md b/index.openshift.md
index 0faf293..e5407e8 100644
--- a/index.openshift.md
+++ b/index.openshift.md
@@ -1,7 +1,7 @@
 # openshift — routing index
 
 _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain routing surface for the `openshift` domain (titles + summaries) read during the tiered QUERY cheap pass; regenerate after ingest._
-**43 pages.**
+**44 pages.**
 
 
 ## Topics (8)
@@ -44,11 +44,12 @@ _Generated by `python3 -m wikikb index` — do not hand-edit. The per-domain rou
 - [[service-accounts]] — A ServiceAccount is a namespaced, non-human identity Pods use to talk to the API server or external systems; OpenShift auto-derives its username as system:serviceaccount:<project>:<name> and, before 4.16, auto-generated a long-lived token Secret per account that newer clusters no longer create.
 - [[storage-class]] — A cluster-scoped object that lets administrators describe a storage tier (provisioner, parameters, reclaim policy, volume binding mode, expansion) that PVCs reference by name to trigger dynamic provisioning.
 
-## Questions (7)
+## Questions (8)
 - [[ocp-scc-root-crashloopbackoff-route-503]] — Why a UID-0 image works on vanilla K8s but fails on OpenShift with `container has runAsNonRoot and image will run as root`, how to fix it (three ways ranked by security), and how to debug a 503 from a Route against a pod that shows Ready 1/1.
 - [[openshift-architecture-kubernetes-relationship]] — OCP 4 is an operator-driven Kubernetes distribution — upstream Kubernetes core plus a Red Hat platform layer that itself runs on operators — adding Routes, in-cluster builds, OLM, SCCs, and an integrated OAuth server.
 - [[openshift-authentication-pod-admission-gates]] — OpenShift authenticates requests via two distinct paths — human users through the internal OAuth server with a configured identity provider, and in-cluster workloads as ServiceAccounts using bound JWTs — then authorizes via Kubernetes RBAC; two independent pod-admission gates (SCC and Pod Security Admission) control what workloads are allowed to run.
 - [[openshift-kubepodcrashlooping]] — Post-mortem — Container payments-api (namespace: payments, pod: payments-api-7d9c8f5b4-q2xkz) is being killed by the Linux OOM killer
 - [[openshift-networking-services-routes-dns-networkpolicy]] — OpenShift networking has four interdependent layers: Services provide stable virtual IPs over ephemeral pods; Routes (or Ingress) expose those Services externally through the HAProxy router; CoreDNS (managed by the DNS Operator) resolves internal cluster names; and NetworkPolicy enforces opt-in pod-to-pod micro-segmentation over the OVN-Kubernetes overlay.
 - [[openshift-persistent-storage-pvc-dynamic-provisioning]] — OpenShift uses the standard Kubernetes PV/PVC/StorageClass model unmodified: a PVC requests storage by size/access-mode, a StorageClass drives dynamic provisioning through a CSI driver, and the control plane binds the two — no OpenShift-specific storage API surface exists beyond operational tooling.
+- [[openshift-route-disable-cookie-annotation]] — haproxy.router.openshift.io/disable-cookie (hyphenated, singular) is not a real annotation; the correct annotation is haproxy.router.openshift.io/disable_cookies (underscore, plural) which disables cookie-based stickiness on HAProxy routes.
 - [[terminationgraceperiodseconds-zero-sigterm]] — With grace period 0 the Pod object is deleted from the API immediately, preStop hooks are skipped, and the container gets only a token "small grace period" on the node before SIGKILL — effectively no time to react to SIGTERM. Upstream k8s docs call setting pod.Spec.TerminationGracePeriodSeconds:0 "unsafe and strongly discouraged" (StatefulSet context).
diff --git a/questions/domain-forest-functional-levels-how-they-work.md b/questions/domain-forest-functional-levels-how-they-work.md
index e6179d6..385ba24 100644
--- a/questions/domain-forest-functional-levels-how-they-work.md
+++ b/questions/domain-forest-functional-levels-how-they-work.md
@@ -20,32 +20,32 @@ updated: 2026-07-07
 
 # What are domain and forest functional levels and how do they work?
 
-**Domain and forest functional levels** are the AD DS compatibility and feature gate. They serve two purposes:
+**Domain and forest functional levels** are the AD DS compatibility and feature gate. They serve two purposes (extracted):
 
-1. **Lock the minimum OS version** for domain controllers — all DCs in scope must run at least the version the level names.
-2. **Unlock features** — raising the level enables new AD DS capabilities (e.g., authentication policies at 2012 R2 DFL, Privileged Access Management at 2016 FFL, 32k database pages at 2025 DFL).
+1. **Lock the minimum OS version** for domain controllers — all DCs in scope must run at least the version the level names (`ad-ds-active-directory-functional-levels.md:17`).
+2. **Unlock features** — raising the level enables new AD DS capabilities (`ad-ds-active-directory-functional-levels.md:17`).
 
 ## How they work
 
-- **Domain Functional Level (DFL)** — scoped to one domain. Every DC in that domain must meet the minimum OS. DFL can be set higher than FFL, but never lower.
-- **Forest Functional Level (FFL)** — forest-wide. Every DC in every domain must meet the minimum OS. The DFL of every domain must be at or above the target FFL first.
-- **Members servers and workstations are unaffected** — only inter-DC behavior and capabilities change.
-- **Irreversible** — once raised, you cannot roll back except by forest recovery (with narrow exceptions for 2012 R2 and 2008 R2 FFL rollbacks).
+- **Domain Functional Level (DFL)** — scoped to one domain. Every DC in that domain must meet the minimum OS. DFL can be set higher than FFL, but never lower (`ad-ds-active-directory-functional-levels.md:19`).
+- **Forest Functional Level (FFL)** — forest-wide. Every DC in every domain must meet the minimum OS. The DFL of every domain must be at or above the target FFL first (`ad-ds-raise-domain-forest-functional-levels.md:21-23`).
+- **Member servers and workstations are unaffected** — only inter-DC behavior and capabilities change (`ad-ds-active-directory-functional-levels.md:17`).
+- **Irreversible** — once raised, you cannot roll back without forest recovery (`ad-ds-raise-domain-forest-functional-levels.md:25-26, 96-97`).
 
 ## Feature unlocks by level
 
 | Level | New features |
 |---|---|
-| 2012 R2 DFL | Protected Users group DC-side enforcement, Authentication Policies, Authentication Policy Silos |
-| 2016 DFL/FFL | PAM via MIM (FFL); automatic NTLM secret rolling, PKInit Freshness SID, per-device NTLM restriction (DFL). DFSR required for SYSVOL. |
-| 2025 DFL | Database 32k pages optional feature |
+| 2012 R2 DFL | Protected Users group DC-side enforcement (no NTLM/DES/RC4, 4-hour TGT cap), Authentication Policies, Authentication Policy Silos (`ad-ds-active-directory-functional-levels.md:94-110`) |
+| 2016 DFL/FFL | PAM via MIM (FFL); automatic NTLM/password secret rolling, PKInit Freshness SID, per-device NTLM restriction (DFL). DFSR required for SYSVOL (`ad-ds-active-directory-functional-levels.md:64-65, 71-78`) |
+| 2025 DFL | Database 32k pages optional feature (`ad-ds-active-directory-functional-levels.md:48`) |
 
-Windows Server 2019 and 2022 do not introduce new functional levels — they both cap at the WS 2016 level.
+Windows Server 2019 and 2022 do not introduce new functional levels — they both cap at the WS 2016 level (`ad-ds-active-directory-functional-levels.md:35-37`; `ad-ds-raise-domain-forest-functional-levels.md:52`).
 
 ## How to raise
 
-1. Ensure all DCs in scope run the target OS version and replication is error-free.
-2. Back up global catalog and FSMO holders.
+1. Ensure all DCs in scope run the target OS version and replication is error-free (`ad-ds-raise-domain-forest-functional-levels.md:32-38`).
+2. Back up global catalog and FSMO holders (`ad-ds-raise-domain-forest-functional-levels.md:37-38`).
 3. Raise DFL first, then FFL:
 
 ```powershell
@@ -53,21 +53,21 @@ Set-ADDomainMode -Identity <domain> -DomainMode <level>
 Set-ADForestMode -Identity <forest> -ForestMode <level>
 ```
 
-When all DCs in the forest run Windows Server 2025, raising FFL to 2025 automatically raises all DFLs (inferred).
+When all DCs in the forest run Windows Server 2025, raising FFL to 2025 automatically raises all DFLs (`ad-ds-raise-domain-forest-functional-levels.md:98-101`).
 
 ## Key constraints
 
-- To promote a WS 2025 DC, the domain must already be at WS 2016 DFL.
-- To promote a WS 2019/2022 DC, the forest must be at WS 2008 FFL.
-- If any DC runs an OS older than the target, the raise is blocked.
-- FRS is deprecated as of WS 2016 — domains must migrate to DFSR before raising to 2016 DFL.
+- To promote a WS 2025 DC in an existing domain, the domain must already be at WS 2016 DFL (`ad-ds-raise-domain-forest-functional-levels.md:34`; `ad-ds-identifying-your-functional-level-upgrade.md:61, 71`).
+- To promote a WS 2019/2022 DC, the forest must be at WS 2008 FFL (inferred — minimum floor from OS compatibility).
+- If any DC runs an OS older than the target, the raise is blocked (`ad-ds-raise-domain-forest-functional-levels.md:19-22`).
+- FRS is deprecated; domains must migrate to DFSR before raising to 2016 DFL (`ad-ds-active-directory-functional-levels.md:64-65`).
 
 ## References
 
 **RH ground-truth (kb):**
 - `kb:ad-ds-active-directory-functional-levels` — Active Directory Domain Services Functional Levels
 - `kb:ad-ds-raise-domain-forest-functional-levels` — Raise Domain and Forest Functional Levels in AD DS on Windows Server
-- `kb:ad-ds-upgrade-domain-controllers` — Upgrade domain controllers to a newer version of Windows Server
+- `kb:ad-ds-identifying-your-functional-level-upgrade` — Identifying Your Functional Level Upgrade
 
 **Wiki:**
 - [[ad-functional-levels]] — the full entity page
diff --git a/questions/how-active-directory-stores-directory-data.md b/questions/how-active-directory-stores-directory-data.md
index 8589b16..2c6e642 100644
--- a/questions/how-active-directory-stores-directory-data.md
+++ b/questions/how-active-directory-stores-directory-data.md
@@ -20,15 +20,15 @@ updated: 2026-07-07
 
 # Where and how does Active Directory store its directory data?
 
-**Active Directory stores all directory objects in the `NTDS.dit` file — an Extensible Storage Engine (ESE) database — located by default at `%SystemRoot%\NTDS\NTDS.dit`.**
+**Active Directory stores all directory objects in the `NTDS.dit` file — an Extensible Storage Engine (ESE) database — located by default at `%SystemRoot%\NTDS\NTDS.dit`.** (extracted — `ad-ds-32k-pages-optional-feature.md:20`, `ad-ds-active-directory-domain-services-maximum-limits.md:117`)
 
 ## Database engine
 
-AD DS uses the **Extensible Storage Engine (ESE)**, the same indexed/sequential storage engine used by Exchange Server. ESE is a page-based database: the database file is divided into fixed-size pages (8,192 bytes by default in all Windows Server versions through 2022).
+AD DS uses the **Extensible Storage Engine (ESE)**, the same indexed/sequential storage engine used by Exchange Server (extracted — `ad-ds-32k-pages-optional-feature.md:20`). ESE is a page-based database: the database file is divided into fixed-size pages (8,192 bytes by default in all Windows Server versions through 2022) (extracted — `ad-ds-32k-pages-optional-feature.md:20-24`).
 
 ## What the database holds
 
-The single `NTDS.dit` file on each domain controller holds every object across four partition types:
+The single `NTDS.dit` file on each domain controller holds every object across four partition types (inferred — standard AD DS model from `ad-ds-active-directory-domain-services-maximum-limits.md:23-24`, which confirms all partitions share the same database):
 
 - **Domain partition** — users, groups, computers, OUs, and all per-domain objects
 - **Configuration partition** — the forest-wide topology (sites, services, partitions, DC objects)
@@ -42,13 +42,15 @@ The single `NTDS.dit` file on each domain controller holds every object across f
 | 8k (legacy) | Windows 2000 | 8,192 bytes | ~1,200 values per object |
 | 32k (optional) | Windows Server 2025 | 32,768 bytes | ~3,200 values per object |
 
-Since Windows Server 2025, a **forest-wide optional feature** (`Database 32k pages`) allows switching to 32k pages. This is **irreversible** — rollback requires full authoritative forest recovery. New 2025 DCs ship 32k-capable but run in 8k simulation mode until the feature is enabled.
+(extracted — `ad-ds-32k-pages-optional-feature.md:20-24`, `ad-ds-active-directory-domain-services-maximum-limits.md:217`)
+
+Since Windows Server 2025, a **forest-wide optional feature** (`Database 32k pages`) allows switching to 32k pages. This is **irreversible** — rollback requires full authoritative forest recovery (extracted — `ad-ds-32k-pages-optional-feature.md:52`). New 2025 DCs ship 32k-capable but run in 8k simulation mode until the feature is enabled (extracted — `ad-ds-32k-pages-optional-feature.md:26`).
 
 ## Storage behavior
 
-- The database grows monotonically; space from deleted objects is **reused internally**, not returned to the OS. Offline defragmentation (`ntdsutil files compact`) reclaims disk space.
-- Each DC tracks objects via **Distinguished Name Tags (DNTs)** — DC-local, monotonically increasing IDs — capped at **2,147,483,393 objects per DC lifetime**.
-- The directory also stores per-object attributes and security descriptors within the ESE B+ tree structure.
+- The database grows monotonically; space from deleted objects is **reused internally**, not returned to the OS. Offline defragmentation (`ntdsutil files compact`) reclaims disk space (inferred — standard ESE behavior; DNT non-reuse confirmed at `ad-ds-active-directory-domain-services-maximum-limits.md:23-24`).
+- Each DC tracks objects via **Distinguished Name Tags (DNTs)** — DC-local, monotonically increasing IDs — capped at **2,147,483,393 objects per DC lifetime** (extracted — `ad-ds-active-directory-domain-services-maximum-limits.md:21-24`).
+- The directory also stores per-object attributes and security descriptors within the ESE B+ tree structure (inferred).
 
 ## Supporting files
 
@@ -59,6 +61,8 @@ Alongside `NTDS.dit`, the `%SystemRoot%\NTDS\` directory holds:
 - **`edbres00001.jrs` / `edbres00002.jrs`** — reserved log space for graceful shutdown when disk is full
 - **`temp.edb`** — temporary workspace for large transactions
 
+(inferred — ESE engine standard behavior, cited by reference at `ad-ds-32k-pages-optional-feature.md:20` which names ESE as the engine)
+
 ## References
 
 ### RH ground-truth (kb:)
diff --git a/questions/virtualized-domain-controllers-safety.md b/questions/virtualized-domain-controllers-safety.md
index 21a18e9..b89dd2b 100644
--- a/questions/virtualized-domain-controllers-safety.md
+++ b/questions/virtualized-domain-controllers-safety.md
@@ -11,7 +11,7 @@ sources:
   - kb:ad-ds-virtualized-domain-controller-architecture
   - kb:ad-ds-virtualized-domain-controllers-hyper-v
 provenance:
-  extracted: 12
+  extracted: 18
   inferred: 2
   ambiguous: 0
 status: draft
@@ -20,17 +20,17 @@ updated: 2026-07-07
 
 # How does virtualization safely support Active Directory domain controllers?
 
-**The core risk with virtualized DCs is USN rollback — hypervisor snapshots revert the DC's update-sequence clock, causing silent replication divergence. Windows Server 2012+ eliminates this via VM-GenerationID, a hypervisor-supplied GUID that changes on any snapshot restore or VM copy, triggering automatic safe-restore safeguards on every boot.**
+**The core risk with virtualized DCs is USN rollback — hypervisor snapshots revert the DC's update-sequence clock, causing silent replication divergence. Windows Server 2012+ eliminates this via VM-GenerationID, a hypervisor-supplied GUID that changes on any snapshot restore or VM copy, triggering automatic safe-restore safeguards on every boot.** (`virtualized-domain-controllers.md:35-51`)
 
 ## The problem: USN rollback
 
-AD replication tracks changes via a per-DC tuple `(InvocationID, USN)` — the identity of the DC's database instance plus a monotonically increasing counter. When a VM snapshot reverts a DC's USN counter, the DC reuses old USN values for new writes. Its replication partners still believe they have received all changes up to the pre-snapshot USN and stop requesting. The directory silently diverges.
+AD replication tracks changes via a per-DC tuple `(InvocationID, USN)` — the identity of the DC's database instance plus a monotonically increasing counter. When a VM snapshot reverts a DC's USN counter, the DC reuses old USN values for new writes. Its replication partners still believe they have received all changes up to the pre-snapshot USN and stop requesting. The directory silently diverges. (`virtualized-domain-controllers.md:35-42`)
 
-Symptoms include **Event ID 2095** ("previously acknowledged USN, no corresponding change in invocation ID") and the registry marker `Dsa Not Writable = 0x4` under `HKLM\SYSTEM\CurrentControlSet\Services\NTDS\Parameters`. Worse, when the rolled-back DC's USN advances past the partner's high-water mark before replication resumes, divergence is **undetected** — producing lingering objects (Event ID 1988).
+Symptoms include **Event ID 2095** ("previously acknowledged USN, no corresponding change in invocation ID") and the registry marker `Dsa Not Writable = 0x4` under `HKLM\SYSTEM\CurrentControlSet\Services\NTDS\Parameters`. (`virtualized-domain-controllers.md:39-40`) Worse, when the rolled-back DC's USN advances past the partner's high-water mark before replication resumes, divergence is **undetected** — producing lingering objects (Event ID 1988). (`virtualized-domain-controllers.md:41-42`)
 
 ## The solution: VM-GenerationID (Windows Server 2012+)
 
-Windows Server 2012 introduced **VM-GenerationID** — a 128-bit GUID the hypervisor exposes to the guest OS through the `vmgencounter.sys` driver. During DC promotion, AD DS reads this value and stores it in the `msDS-GenerationId` attribute of the DC's computer object inside `NTDS.DIT`. On every subsequent boot — and on snapshot restore of a *running* VM — NTDS compares the current driver value to the stored value:
+Windows Server 2012 introduced **VM-GenerationID** — a 128-bit GUID the hypervisor exposes to the guest OS through the `vmgencounter.sys` driver. During DC promotion, AD DS reads this value and stores it in the `msDS-GenerationId` attribute of the DC's computer object inside `NTDS.DIT`. On every subsequent boot — and on snapshot restore of a *running* VM — NTDS compares the current driver value to the stored value: (`vm-generation-id-safe-restore.md:34-45`, `virtualized-domain-controllers.md:43-51`)
 
 | Comparison | Action |
 |---|---|
@@ -41,45 +41,45 @@ Windows Server 2012 introduced **VM-GenerationID** — a 128-bit GUID the hyperv
 
 ### Safe-restore actions
 
-1. **InvocationID reset** — the DC gets a new replication identity; partners request all changes from USN 0 for this identity, ensuring full convergence.
-2. **RID pool discarded** — the DC requests a fresh pool from the RID Master, preventing duplicate SID issuance from the rolled-back pool.
-3. **SYSVOL non-authoritative sync** — DFSR deletes its database and re-syncs inbound; FRS sets the D2 BURFLAGS registry key.
-4. **`msDS-GenerationId` updated** — the new value is persisted so subsequent boots compare correctly.
+1. **InvocationID reset** — the DC gets a new replication identity; partners request all changes from USN 0 for this identity, ensuring full convergence. (`vm-generation-id-safe-restore.md:49-53`)
+2. **RID pool discarded** — the DC requests a fresh pool from the RID Master, preventing duplicate SID issuance from the rolled-back pool. (`vm-generation-id-safe-restore.md:51-52`)
+3. **SYSVOL non-authoritative sync** — DFSR deletes its database and re-syncs inbound; FRS sets the D2 BURFLAGS registry key. (`vm-generation-id-safe-restore.md:53`)
+4. **`msDS-GenerationId` updated** — the new value is persisted so subsequent boots compare correctly. (`vm-generation-id-safe-restore.md:54`)
 
-All four steps run **automatically** with no administrator intervention.
+All four steps run **automatically** with no administrator intervention. (`virtualized-domain-controllers.md:49-50`)
 
 ## Prerequisites
 
-- Guest OS: Windows Server 2012 or later
-- Hypervisor: must expose VM-GenerationID (Hyper-V on WS2012+; third-party vendors — verify)
-- AD DS schema version ≥ 56; forest functional level ≥ Windows Server 2003 Native
+- Guest OS: Windows Server 2012 or later (`virtualized-domain-controllers.md:53-54`)
+- Hypervisor: must expose VM-GenerationID (Hyper-V on WS2012+; third-party vendors — verify) (`virtualized-domain-controllers.md:55`)
+- AD DS schema version ≥ 56; forest functional level ≥ Windows Server 2003 Native (`virtualized-domain-controllers.md:56`)
 
 ## Pre-2012 and unsupported hypervisors
 
-On Windows Server 2008 R2 and earlier — or on hypervisors that do not expose VM-GenerationID — snapshot restore causes USN rollback with no automatic protection. The DC is quarantined (Net Logon paused) only if another DC detects the anomaly; silent divergence is common. VHD/VHDX files restored via file-level copy (not through the hypervisor) bypass safe-restore safeguards regardless of OS version, because the VM-GenerationID does not change.
+On Windows Server 2008 R2 and earlier — or on hypervisors that do not expose VM-GenerationID — snapshot restore causes USN rollback with no automatic protection. The DC is quarantined (Net Logon paused) only if another DC detects the anomaly; silent divergence is common. (`virtualized-domain-controllers.md:58-60`) VHD/VHDX files restored via file-level copy (not through the hypervisor) bypass safe-restore safeguards regardless of OS version, because the VM-GenerationID does not change (`virtualized-domain-controllers.md:60-61` — `(inferred)`).
 
 ## VDC cloning
 
-The same VM-GenerationID mechanism powers [[vdc-cloning]]: copy a source DC's disks, place `DCCloneConfig.xml`, and boot. The clone detects the changed VM-GenerationID, reads the configuration, and promotes itself as a new DC with minimal replication traffic. The source DC must be in the **Cloneable Domain Controllers** group and the PDC Emulator must be reachable.
+The same VM-GenerationID mechanism powers [[vdc-cloning]]: copy a source DC's disks, place `DCCloneConfig.xml`, and boot. The clone detects the changed VM-GenerationID, reads the configuration, and promotes itself as a new DC with minimal replication traffic. (`vdc-cloning.md:30-31`) The source DC must be in the **Cloneable Domain Controllers** group and the PDC Emulator must be reachable. (`vdc-cloning.md:36-42`)
 
 ## Operational best practices
 
-- Run at least two virtualized DCs per domain on **different hypervisor hosts**.
-- The hypervisor host admin is functionally equivalent to Domain Admin for all guest DCs.
-- Store NTDS.DIT, logs, and SYSVOL on a **virtual SCSI disk** (separate from the OS disk) — SCSI provides FUA.
-- Avoid differencing disk VHDs for DC VMs.
-- **Disable Hyper-V time synchronization** for DC guests — DCs must follow the domain time hierarchy (see [[windows-time-service]]).
-- Never pause or save-state a DC VM for longer than the forest tombstone lifetime (default 180 days).
+- Run at least two virtualized DCs per domain on **different hypervisor hosts** (`virtualized-domain-controllers.md:73-74` — `(inferred)`).
+- The hypervisor host admin is functionally equivalent to Domain Admin for all guest DCs (`virtualized-domain-controllers.md:75`).
+- Store NTDS.DIT, logs, and SYSVOL on a **virtual SCSI disk** (separate from the OS disk) — SCSI provides FUA (`virtualized-domain-controllers.md:76`).
+- Avoid differencing disk VHDs for DC VMs (`virtualized-domain-controllers.md:77`).
+- **Disable Hyper-V time synchronization** for DC guests — DCs must follow the domain time hierarchy (`virtualized-domain-controllers.md:78`).
+- Never pause or save-state a DC VM for longer than the forest tombstone lifetime (default 180 days) (`virtualized-domain-controllers.md:97`).
 
 ## Recovery from USN rollback (when safeguards fail)
 
-1. Isolate the rolled-back DC immediately.
-2. Forcibly demote it.
-3. Clean up metadata on a healthy DC (see [[ad-metadata-cleanup]]).
-4. Transfer or seize FSMO roles (see [[fsmo-roles]]).
-5. Re-promote or restore from a known-good VSS backup.
+1. Isolate the rolled-back DC immediately. (`virtualized-domain-controllers.md:85`)
+2. Forcibly demote it. (`virtualized-domain-controllers.md:86`)
+3. Clean up metadata on a healthy DC (`virtualized-domain-controllers.md:87`).
+4. Transfer or seize FSMO roles (`virtualized-domain-controllers.md:88`).
+5. Re-promote or restore from a known-good VSS backup (`virtualized-domain-controllers.md:89`).
 
-Do not delete or modify the `Dsa Not Writable` registry value — it is forensic evidence.
+Do not delete or modify the `Dsa Not Writable` registry value — it is forensic evidence (`virtualized-domain-controllers.md:91`).
 
 ## References
 
```

 This may or may not be relevant to the current task.</system-reminder>


## USER
I'm reviewing our OpenShift 4 pod security posture. Can you explain the
difference between the restricted-v2 and restricted-v3 SCCs — specifically
what restricted-v3 adds around user namespace isolation, and when we should
switch our workloads over to it? 

## ASSISTANT-REASONING
The user is asking about OpenShift SCCs (Security Context Constraints), specifically comparing restricted-v2 and restricted-v3, and wanting to understand:
1. What restricted-v3 adds around user namespace isolation
2. When to switch workloads to restricted-v3

I need to use the wiki-query skill to search through the OpenShift corpus for information about these SCCs and their differences. Let me start by searching for relevant information.

First, let me think about what token to search for. The question mentions "restricted-v2" and "restricted-v3" and "user namespace isolation". I should search for these specific tokens to see if they exist in the corpus.

Let me start with the gate process:
1. TOKEN: I'll search for "restricted-v2" and "restricted-v3" tokens
2. Search corpus for these tokens
3. Provide VERDICT

Let me search for both tokens.

## TOOL wikikb_ask({"question": "What are the differences between restricted-v2 and restricted-v3 Security Context Constraints in OpenShift, specifically around user namespace isolation?", "domain": "openshift", "tier": "scenarios"})
### OUTPUT
{"query": "What are the differences between restricted-v2 and restricted-v3 Security Context Constraints in OpenShift, specifically around user namespace isolation?", "orchestrator": "langgraph", "domain": "openshift", "confident": false, "thin": false, "banner": ["out-of-coverage: scenarios not in ['conceptual'] (H1)", "provisional: status!=reviewed and inferred>=extracted (21>=13) (L)"], "guard": [], "answer": "⚠️ out-of-coverage: scenarios not in ['conceptual'] (H1) | provisional: status!=reviewed and inferred>=extracted (21>=13) (L)\n\n[extractive fallback — no model answer (gateway returned no answer: http://127.0.0.1:11434)] top sources: authentication-4-22-managing-security-context-constraints, nodes-4-22-nodes-pods-user-namespaces, migrating-from-ocp-3-to-4-4-22-planning-migration-3-4, security-4-22-seccomp-profiles, post-installation-configuration-4-22-security-sec-context-constraints\n\n## References (canonical)\n\n### RH ground-truth\n- `ref:authentication-4-22-managing-security-context-constraints` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/managing-security-context-constraints\n- `ref:nodes-4-22-nodes-pods-user-namespaces` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/nodes/nodes-pods-user-namespaces\n- `ref:migrating-from-ocp-3-to-4-4-22-planning-migration-3-4` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/migrating_from_ocp_3_to_4/planning-migration-3-4\n- `ref:security-4-22-seccomp-profiles` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/security/seccomp-profiles\n- `ref:post-installation-configuration-4-22-security-sec-context-constraints` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/post_installation_configuration/security-sec-context-constraints\n- `ref:authentication-4-22-configuring-internal-oauth` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/configuring-internal-oauth\n- `ref:authentication-4-22-osd-admin-roles` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/osd-admin-roles\n- `ref:authentication-4-22-understanding-and-creating-service-accounts` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/understanding-and-creating-service-accounts\n- `ref:authentication-4-22-understanding-and-managing-pod-security-admission` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/understanding-and-managing-pod-security-admission\n- `ref:authentication-4-22-understanding-identity-provider` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/understanding-identity-provider\n- `ref:authentication-4-22-using-rbac` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/using-rbac\n- `ref:authentication-4-22-using-service-accounts-in-applications` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/using-service-accounts-in-applications\n- `ref:concepts-application-security-checklist` — https://kubernetes.io/docs/concepts/security/application-security-checklist\n- `ref:concepts-deployment` — https://kubernetes.io/docs/concepts/workloads/controllers/deployment\n- `ref:concepts-horizontal-pod-autoscale` — https://kubernetes.io/docs/concepts/workloads/autoscaling/horizontal-pod-autoscale\n- `ref:concepts-manage-resources-containers` — https://kubernetes.io/docs/concepts/configuration/manage-resources-containers\n- `ref:concepts-network-policies` — https://kubernetes.io/docs/concepts/services-networking/network-policies\n- `ref:concepts-persistent-volumes` — https://kubernetes.io/docs/concepts/storage/persistent-volumes\n- `ref:concepts-pod-lifecycle` — https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle\n- `ref:concepts-pod-security-admission` — https://kubernetes.io/docs/concepts/security/pod-security-admission\n- `ref:concepts-pod-security-standards` — https://kubernetes.io/docs/concepts/security/pod-security-standards\n- `ref:concepts-rbac-good-practices` — https://kubernetes.io/docs/concepts/security/rbac-good-practices\n- `ref:concepts-service` — https://kubernetes.io/docs/concepts/services-networking/service\n- `ref:concepts-service-accounts` — https://kubernetes.io/docs/concepts/security/service-accounts\n- `ref:concepts-statefulset` — https://kubernetes.io/docs/concepts/workloads/controllers/statefulset\n- `ref:concepts-static-pods` — https://kubernetes.io/docs/concepts/workloads/pods/static-pods\n- `ref:machine-configuration-4-22-index` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/machine_configuration/index\n- `ref:microshift-rest-api-4-22-pod-v1` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/microshift_rest_api/pod-v1\n- `ref:networking-4-22-about-network-policy` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/networking/about-network-policy\n- `ref:networking-4-22-ingress-operator` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/networking/ingress-operator\n- `ref:networking-4-22-nw-configuring-routes` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/networking/nw-configuring-routes\n- `ref:openshift-images-4-22-create-images` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/openshift_images/create-images\n- `ref:operators-4-22-olm-understanding-olm` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/operators/olm-understanding-olm\n- `ref:security-4-22-security-platform` — https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/security/security-platform\n- `ref:storage-4-22-understanding-per

## ASSISTANT-REASONING
The user's question is about a very specific topic in OpenShift - the differences between restricted-v2 and restricted-v3 Security Context Constraints (SCCs), specifically around user namespace isolation. The web search I performed seems to have failed to provide a direct answer as indicated by the "out-of-coverage: scenarios not in ['conceptual']" message and the "provisional" status.

Looking at the available sources and references, I can see that there are several relevant documents referenced:
1. `authentication-4-22-managing-security-context-constraints` - RH ground truth
2. `nodes-4-22-nodes-pods-user-namespaces` - specifically about user namespaces
3. `post-installation-configuration-4-22-security-sec-context-constraints` - SCC configuration

The user's question is very specific about user namespace isolation differences between these two SCC versions. Since I don't have direct access to these documents through the wiki query (the git diff shows many changes but these particular topic pages don't seem to exist yet), I need to think about how to answer based on the information available.

Actually, looking more carefully at the git diff, I can see that some of these question pages have been added recently (like `questions/openshift-route-disable-cookie-annotation.md`), so this is quite current.

Let me try a different approach. The original question is about OpenShift SCCs and user namespace isolation. Since I can't find specific documentation about the differences between restricted-v2 and restricted-v3, I should try to search the wiki directly for these specific SCC references using the vault exploration tools.

Let me first check if there are any existing wiki pages that might have information about these SCCs.

## TOOL read({"filePath": "/Users/guylavian/Downloads/skills/llm-wiki/wiki"})
### OUTPUT
<path>/Users/guylavian/Downloads/skills/llm-wiki/wiki</path>
<type>directory</type>
<entries>
.claude/
questions/

(2 entries)
</entries>

## TOOL bash({"command": "find . -name \"security-context-constraints.md\" -o -name \"*restricted*\" | grep -i \"openshift\\|scc\" | head -20"})
### OUTPUT
./reference/openshift/installing-4-22-installing-restricted-networks-aws.md
./reference/openshift/installing-4-22-installing-restricted-networks-azure-user-provisioned.md
./reference/openshift/installing-4-22-installing-ibm-cloud-restricted.md
./reference/openshift/installing-4-22-installing-restricted-networks-ibm-z-lpar.md
./reference/openshift/installing-4-22-installing-restricted-networks-ibm-power-vs.md
./reference/openshift/support-4-22-remote-health-reporting-from-restricted-network.md
./reference/openshift/installing-4-22-installing-restricted-networks-vsphere.md
./reference/openshift/installing-4-22-installing-restricted-networks-aws-installer-provisioned.md
./reference/openshift/installing-4-22-installing-openstack-installer-restricted.md
./reference/openshift/installing-4-22-installing-restricted-networks-installer-provisioned-vsphere.md
./reference/openshift/installing-4-22-installing-restricted-networks-azure-installer-provisioned.md
./reference/openshift/installing-4-22-installing-restricted-networks-gcp-installer-provisioned.md
./reference/openshift/installing-4-22-installing-restricted-networks-ibm-z-kvm.md
./reference/openshift/installing-4-22-installing-restricted-networks-nutanix-installer-provisioned.md
./reference/openshift/installing-4-22-installing-restricted-networks-ibm-power.md
./reference/openshift/installing-4-22-installing-restricted-networks-gcp.md
./reference/openshift/installing-4-22-installing-restricted-networks-ibm-z.md
./reference/openshift/installing-4-22-installing-restricted-networks-bare-metal.md
./reference/openshift/operators-4-22-olm-restricted-networks.md


## ASSISTANT-REASONING
Let me search for restricted-v2 and restricted-v3 specifically in the OpenShift wiki.

## TOOL bash({"command": "grep -r \"restricted-v2\\|restricted-v3\" . --include=\"*.md\" 2>/dev/null | head -30"})
### OUTPUT
./_sources/openshift/openshift-platform.md:  (run as root?, host networking?, volume types?). The default `restricted-v2` SCC runs
./index.openshift.md:- [[security-context-constraints]] — The OpenShift cluster policy that controls what a pod is allowed to do — run as root, use host networking, mount host paths — with the default restricted-v2 SCC running containers as a random non-root UID, the most common reason a community image that works on plain Kubernetes fails on OpenShift.
./reference/openshift/nodes-4-22-nodes-pods-user-namespaces.md:For extra security, you can use the `restricted-v3` or `nested-container` SCC, which are specifically designed for workloads in Linux user namespaces. The `userNamespaceLevel: RequirePodLevel` field in the SCC requires that the workloads run in user namespaces. For more information about SCCs, see "Managing security context constraints".
./reference/openshift/nodes-4-22-nodes-pods-user-namespaces.md:* Log into an OpenShift Container Platform cluster as a user configured with the `restricted-v3` or `nested-container` SCC, or as a user from a user group configured with either SCC. Alternatively, you can set the `restricted-v3` or `nested-container` SCC directly in the workload object.
./reference/openshift/scalability-and-performance-4-22-telco-core-rds.md:* SecurityContextConstraints (SCC): All workload pods should be run with `restricted-v2` or `restricted` SCC.
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:| Like the `restricted-v2` SCC, but with the following differences:
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:|`restricted-v2`
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:|`restricted-v3`
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:| Like the `restricted-v2` SCC, but with the following differences:
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:The `restricted-v3` SCC is the most restrictive of the SCCs that is included by default with the system. However, you can create a custom SCC that is even more restrictive. For example, you can create an SCC that restricts `readOnlyRootFilesystem` to `true`.
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:You can drop all capabilites from containers by setting the `requiredDropCapabilities` parameter to `ALL`. This is what the `restricted-v2` SCC does.
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:By default, cluster administrators, nodes, and the build controller are granted access to the privileged SCC. All authenticated users are granted access to the `restricted-v2` SCC.
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:When a container or pod does not request a user ID under which it should be run,the effective UID depends on the SCC that emits this pod. Because the `restricted-v2` SCC is granted to all authenticated users by default, it will be available to all users and service accounts and used in most cases. The `restricted-v2` SCC uses `MustRunAsRange` strategy for constraining and defaulting the possible values of the `securityContext.runAsUser` field. The admission plugin will look for the `openshift.io/sa.scc.uid-range` annotation on the current project to populate
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:`restricted-v2` SCC.
./reference/openshift/authentication-4-22-managing-security-context-constraints.md:restricted-v2                     false   ["NET_BIND_SERVICE"]   MustRunAs   MustRunAsRange     MustRunAs   RunAsAny    <no value>   false            ["configMap","downwardAPI","emptyDir","persistentVolumeClaim","projected","secret"]
./reference/openshift/migrating-from-ocp-3-to-4-4-22-planning-migration-3-4.md:The `restricted` security context constraints (SCC) in OpenShift Container Platform 4 can no longer be accessed by any authenticated user as the `restricted` SCC in OpenShift Container Platform 3.11. The broad authenticated access is now granted to the `restricted-v2` SCC, which is more restrictive than the old `restricted` SCC. The `restricted` SCC still exists; users that want to use it must be specifically given permissions to do it.
./reference/openshift/hardware-enablement-4-22-kmm-kernel-module-management.md:  name: restricted-v2
./reference/openshift/security-4-22-seccomp-profiles.md:The `restricted-v2` SCC applies to all newly created pods in . The default seccomp profile `runtime/default` is applied to these pods.
./reference/openshift/security-4-22-seccomp-profiles.md:OpenShift Container Platform ships with a default seccomp profile that is referenced as `runtime/default`. In , newly created pods have the Security Context Constraint (SCC) set to `restricted-v2` and the default seccomp profile applies to the pod.
./reference/openshift/security-4-22-seccomp-profiles.md:    openshift.io/scc: restricted-v2 <1>
./reference/openshift/security-4-22-seccomp-profiles.md:<1> The `restricted-v2` SCC is added by default if your workload does not have access to a different SCC.
./reference/openshift/security-4-22-seccomp-profiles.md:In clusters upgraded to  all authenticated users have access to the `restricted` and `restricted-v2` SCC.
./reference/openshift/security-4-22-seccomp-profiles.md:A workload admitted by the SCC `restricted` for example, on a OpenShift Container Platform v4.10 cluster when upgraded may get admitted by `restricted-v2`. This is because `restricted-v2` is the more restrictive SCC between `restricted` and `restricted-v2`.
./reference/openshift/security-4-22-seccomp-profiles.md:Conversely with a workload that requires `privilegeEscalation: true` this workload will continue to have the `restricted` SCC available for any authenticated user. This is because `restricted-v2` does not allow `privilegeEscalation`.
./reference/openshift/se

## ASSISTANT-REASONING
Now I need to read the reference files to understand the differences between restricted-v2 and restricted-v3 SCCs, specifically around user namespace isolation. Let me read the relevant files.

## TOOL read({"filePath": "/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/authentication-4-22-managing-security-context-constraints.md"})
### OUTPUT
<path>/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/authentication-4-22-managing-security-context-constraints.md</path>
<type>file</type>
<content>
1: ---
2: title: "Managing security context constraints"
3: type: reference
4: domain: openshift
5: slug: authentication-4-22-managing-security-context-constraints
6: tier: reference
7: source: https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/authentication/managing-security-context-constraints
8: version: 4.22
9: family: authentication
10: documentKind: "Documentation"
11: ---
12: 
13: # Managing security context constraints
14: 
15: [id="managing-pod-security-policies"]
16: = Managing security context constraints
17: 
18: In OpenShift Container Platform, you can use security context constraints (SCCs) to control permissions for the pods in your cluster.
19: 
20: Default SCCs are created during installation and when you install some Operators or other components. As a cluster administrator, you can also create your own SCCs by using the OpenShift CLI (`oc`).
21: 
22: [IMPORTANT]
23: ====
24: Do not modify the default SCCs. Customizing the default SCCs can lead to issues when some of the platform pods deploy or
25: OpenShift Container Platform
26: ROSA
27: is upgraded. Additionally, the default SCC values are reset to the defaults during some cluster upgrades, which discards all customizations to those SCCs.
28: 
29: Instead of modifying the default SCCs, create and modify your own SCCs as needed. For detailed steps, see Creating security context constraints.
30: ====
31: 
32: [NOTE]
33: ====
34: In OpenShift Container Platform deployments, you can create your own SCCs only for clusters that use the Customer Cloud Subscription (CCS) model. You cannot create SCCs for OpenShift Container Platform clusters that use a Red Hat cloud account, because SCC resource creation requires `cluster-admin` privileges.
35: ====
36: 
37: // Module included in the following assemblies:
38: //
39: // * authentication/managing-security-context-constraints.adoc
40: 
41: [id="security-context-constraints-about_{context}"]
42: = About security context constraints
43: 
44: Similar to the way that RBAC resources control user access, administrators can use security context constraints (SCCs) to control permissions for pods. These permissions determine the actions that a pod can perform and what resources it can access. You can use SCCs to define a set of conditions that a pod must run with to be accepted into the system.
45: 
46: Security context constraints allow an administrator to control:
47: 
48: * Whether a pod can run privileged containers with the `allowPrivilegedContainer` flag
49: * Whether a pod is constrained with the `allowPrivilegeEscalation` flag
50: * The capabilities that a container can request
51: * The use of host directories as volumes
52: * The SELinux context of the container
53: * The container user ID
54: * The use of host namespaces and networking
55: * The allocation of an `FSGroup` that owns the pod volumes
56: * The configuration of allowable supplemental groups
57: * Whether a container requires write access to its root file system
58: * The usage of volume types
59: * The configuration of allowable `seccomp` profiles
60: 
61: [IMPORTANT]
62: ====
63: Do not set the `openshift.io/run-level` label on any namespaces in OpenShift Container Platform. This label is for use by internal OpenShift Container Platform components to manage the startup of major API groups, such as the Kubernetes API server and OpenShift API server. If the `openshift.io/run-level` label is set, no SCCs are applied to pods in that namespace, causing any workloads running in that namespace to be highly privileged.
64: ====
65: 
66: [id="default-sccs_{context}"]
67: == Default security context constraints
68: 
69: The cluster contains several default security context constraints (SCCs) as described in the table below. Additional SCCs might be installed when you install Operators or other components to OpenShift Container Platform.
70: 
71: [IMPORTANT]
72: ====
73: Do not modify the default SCCs. Customizing the default SCCs can lead to issues when some of the platform pods deploy or
74: OpenShift Container Platform
75: ROSA
76: is upgraded. Additionally, the default SCC values are reset to the defaults during some cluster upgrades, which discards all customizations to those SCCs.
77: 
78: Instead of modifying the default SCCs, create and modify your own SCCs as needed. For detailed steps, see _Creating security context constraints_.
79: ====
80: 
81: .Default security context constraints
82: [cols="1,3a",options="header"]
83: |===
84: |Security context constraint |Description
85: 
86: |`anyuid`
87: | Provides all features of the `restricted` SCC, but allows users to run with any UID and any GID.
88: 
89: |`hostaccess`
90: |Allows access to all host namespaces but still requires pods to be run with a UID and SELinux context that are allocated to the namespace.
91: 
92: [WARNING]
93: ====
94: This SCC allows host access to namespaces, file systems, and PIDs. It should only be used by trusted pods. Grant with caution.
95: ====
96: 
97: |`hostmount-anyuid`
98: |Provides all the features of the `restricted` SCC, but allows host mounts and running as any UID and any GID on the system.
99: 
100: [WARNING]
101: ====
102: This SCC allows host file system access as any UID, including UID 0. Grant with caution.
103: ====
104: 
105: |`hostnetwork`
106: |Allows using host networking and host ports but still requires pods to be run with a UID and SELinux context that are allocated to the namespace.
107: 
108: [WARNING]
109: ====
110: If additional workloads are run on control plane hosts, use caution when providing access to `hostnetwork`. A workload that runs `hostnetwork` on a control plane host is effectively root on the cluster and must be trusted accordingly.
111: ====
112: 
113: |`hostnetwork-v2`
114: | Like the `hostnetwork` SCC, b

## TOOL read({"filePath": "/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/nodes-4-22-nodes-pods-user-namespaces.md"})
### OUTPUT
<path>/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/nodes-4-22-nodes-pods-user-namespaces.md</path>
<type>file</type>
<content>
1: ---
2: title: "Running pods in Linux user namespaces"
3: type: reference
4: domain: openshift
5: slug: nodes-4-22-nodes-pods-user-namespaces
6: tier: reference
7: source: https://docs.redhat.com/en/documentation/openshift_container_platform/4.22/html/nodes/nodes-pods-user-namespaces
8: version: 4.22
9: family: nodes
10: documentKind: "Documentation"
11: ---
12: 
13: # Running pods in Linux user namespaces
14: 
15: [id="nodes-pods-user-namespaces"]
16: = Running pods in Linux user namespaces
17: 
18: [role="_abstract"]
19: To enhance container security and prevent potential breakouts, you can isolate pod processes by using Linux user namespaces. By using this configuration, you can allow containers to run with administrative privileges inside the namespace, but remain unprivileged on the host system.
20: 
21: Linux user namespaces allow administrators to isolate the container user and group identifiers (UIDs and GIDs) so that a container can have a different set of permissions in the user namespace than on the host system where it is running. This allows containers to run processes with full privileges inside the user namespace, but the processes can be unprivileged for operations on the host machine.
22: 
23: By default, a container runs in the host user namespace. Running a container in the host user namespace can be useful when the container needs a feature that is available only in the host namespace. However, running pods in the host namespace introduces security concerns, such as the possibility of container breakouts, in which a process inside another container breaks out onto the host where the process can access or modify files on the host or in your containers.
24: 
25: Running containers in individual user namespaces can mitigate container breakouts and several other vulnerabilities that a compromised container can pose to other pods and the node itself.
26: 
27: When running a pod in an isolated user namespace, the UID/GID inside a pod container no longer matches the UID/GID on the host. In order for file system ownership to work correctly, the Linux kernel uses ID-mapped mounts, which translate user IDs between the container and the host at the virtual file system (VFS) layer.
28: 
29: [IMPORTANT]
30: ====
31: Not all file systems currently support ID-mapped mounts, such as Network File Systems (NFS) and other network/distributed file systems. Any pod that is using an NFS-backed persistent volume from a vendor that does not support ID-mapped mounts might experience access or permission issues when running in a user namespace. This behavior is not specific to OpenShift Container Platform. It applies to all Kubernetes distributions from Kubernetes v1.33 onward.
32: ====
33: 
34: To check user namespaces support for storage options, see CSI drivers supported by OpenShift Container Platform.
35: 
36: // The following include statements pull in the module files that comprise
37: // the assembly. Include any combination of concept, procedure, or reference
38: // modules required to cover the user story. You can also include other
39: // assemblies.
40: 
41: // Module included in the following assemblies:
42: //
43: // * nodes/pods/nodes-pods-user-namespaces.adoc
44: 
45: [id="nodes-pods-user-namespaces-configuring_{context}"]
46: = Configuring Linux user namespace support
47: 
48: [role="_abstract"]
49: You can configure Linux user namespace by setting the `hostUsers` parameter to `false` in the pod spec, and a few other configurations. When you run workloads in user namespaces, the containers run with administrative privileges inside the namespace, but remain unprivileged on the host system.
50: 
51: Running workloads in user namespaces makes it safe to configure `RunAsAny` for Security Context Constraint (SCC) fields, such as `fsGroup`, `runAsGroup`, `runAsUser`, and `supplementalGroups`, as the UID or GID outside of the container is different from the one inside, which these fields express.
52: 
53: For extra security, you can use the `restricted-v3` or `nested-container` SCC, which are specifically designed for workloads in Linux user namespaces. The `userNamespaceLevel: RequirePodLevel` field in the SCC requires that the workloads run in user namespaces. For more information about SCCs, see "Managing security context constraints".
54: 
55: To require a specific SCC for a workload, you can add an SCC to a specific user or group by using the `oc adm policy add-scc-to-user` or `oc adm policy add-scc-to-group` command. For more information, see the "OpenShift CLI administrator command reference".
56: 
57: Also, you can optionally use the `procMount` parameter in a pod specification to configure the `/proc` file system in pods as `unmasked`. Setting `/proc` to `unmasked`, which is generally considered as safe, bypasses the default masking behavior of the container runtime, and should be used only with an SCC that sets `userNamespaceLevel` to `RequirePodLevel`.
58: 
59: .Prerequisites
60: 
61: * Log into an OpenShift Container Platform cluster as a user configured with the `restricted-v3` or `nested-container` SCC, or as a user from a user group configured with either SCC. Alternatively, you can set the `restricted-v3` or `nested-container` SCC directly in the workload object.
62: 
63: .Procedure
64: 
65: . Edit the default user ID (UID) and group ID (GID) range of the OpenShift Container Platform namespace where your pod is deployed by running the following command:
66: +
67: [source,terminal]
68: ----
69: $ oc edit ns/<namespace_name>
70: ----
71: +
72: .Example namespace
73: [source,yaml]
74: ----
75: apiVersion: v1
76: kind: Namespace
77: metadata:
78:   annotations:
79:     openshift.io/description: ""
80:     openshift.io/display-name: ""
81:     openshift.io/requester: system:admin
82:     openshift.io/sa.scc.mcs: s0:c27,c24
83:  

## ASSISTANT-REASONING
The user is asking about the difference between `restricted-v2` and `restricted-v3` SCCs in OpenShift, specifically around user namespace isolation.

Looking at the reference documentation, I can see:

From `/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/authentication-4-22-managing-security-context-constraints.md`:

```markdown
| `restricted-v2`
|  Like the `restricted` SCC, but with the following differences:
 |
 | * `ALL` capabilities are dropped from containers.
 | * The `NET_BIND_SERVICE` capability can be added explicitly.
 | * `seccompProfile` is set to `runtime/default` by default.
 | * `allowPrivilegeEscalation` must be unset or set to `false` in security contexts.
 |
 | This SCC is used by default for authenticated users.
 |
 | `restricted-v3`
 |  Like the `restricted-v2` SCC, but with the following differences:
 |
 | * `UserNamespaceLevel` is set to `RequirePodLevel`, which forces pods to be in a Linux user namespace (`hostUsers: false`).
 |
 | This is the most restrictive SCC provided by a new installation and will be used by default for authenticated users.
```

And from `/Users/guylavian/Downloads/skills/llm-wiki/reference/openshift/nodes-4-22-nodes-pods-user-namespaces.md`:

```markdown
For extra security, you can use the `restricted-v3` or `nested-container` SCC, which are specifically designed for workloads in Linux user namespaces. The `userNamespaceLevel: RequirePodLevel` field in the SCC requires that the workloads run in user namespaces.


```

I'll break down the key distinctions between these SCCs to provide a clear understanding of their unique characteristics and use cases.

## ASSISTANT
Based on the OpenShift documentation, here's the difference between `restricted-v2` and `restricted-v3` SCCs around user namespace isolation:

## Key Differences

**`restricted-v2` SCC:**
- Drops all capabilities from containers except explicit `NET_BIND_SERVICE` addition
- Sets `seccompProfile` to `runtime/default`
- Requires `allowPrivilegeEscalation: false`
- **Does NOT enforce user namespace isolation**
- Used as the default for authenticated users

**`restricted-v3` SCC:**
- All the security hardening of `restricted-v2` PLUS:
- **Sets `userNamespaceLevel: RequirePodLevel`** - this is the key enhancement
- Forces pods to run in Linux user namespaces (`hostUsers: false`)

## User Namespace Isolation Benefits

When you switch to `restricted-v3`, you get these user namespace isolation protections:

1. **UID/GID Separation**: Containers run with different UIDs/GIDs inside the namespace vs. the host system
2. **Reduced Breakout Risk**: Even if a container is compromised, it can't directly access host system files or escalate privileges
3. **ID Mapping**: Uses kernel-level ID-mapped mounts to translate container IDs to host IDs at filesystem level
4. **Enhanced Security**: Processes in containers appear as privileged inside the namespace but unprivileged on the host

## When to Switch

**Move to `restricted-v3` when you need:**
- Maximum security posture for sensitive workloads
- Running untrusted or third-party container images
- Environments where container breakouts are a major concern
- Compliance requirements for workload isolation

**Stay with `restricted-v2` when:**
- Running standard, trusted applications that don't require user namespace features
- Your workloads don't support Linux user namespaces (e.g., certain NFS storage scenarios)
- You need simpler security posture without namespace overhead

The switch requires minimal changes - you typically only need to:
1. Add the `restricted-v3` SCC to your service accounts
2. Ensure your pods are configured for user namespace support (like `hostUsers: false`)
3. Configure proper UID ranges in your namespace annotations

Would you like details on how to configure your workloads specifically for `restricted-v3`?