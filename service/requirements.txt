# Lexical core — the default image (zero ML deps). Install from the INTERNAL MIRROR only.
#   pip install --no-index --index-url <INTERNAL_MIRROR> -r requirements.txt
fastapi==0.138.1
starlette==1.3.1
uvicorn==0.49.0
pydantic==2.13.4
PyYAML==6.0.3
